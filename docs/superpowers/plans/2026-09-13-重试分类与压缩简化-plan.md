# 重试分类间隔与压缩简化 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重试按错误类别区分等待间隔（429/超时 5s、500 类/空响应 30s）、墙钟上限 20→12 分钟、空响应纳入重试、压缩通道纳入重试并去除递归降级；压缩阈值/保留条数从配置移除（阈值写死 0.65）、新压缩算法按完整回合链累加到 `maxContextTokens×0.65×0.8` 后整体压缩、摘要固定 5000 字；去掉 Esc 停止；新建模型默认 200k 且内置名简化为 deepseek/qwen。

**Architecture:** 重试判定与策略集中在 `RetryPolicy`（分类 Kind + isTransient/kindOf 静态判定，替换两处循环里重复的 `isTransientError`）；新增 `LlmException.Type.EMPTY_RESPONSE` 承载"HTTP 200 但响应体为 null"；`RetryProgress` 增带"下次等待时长"，指示器展示"约 N 秒后重试"。压缩侧 `ContextManager` 由"递归降级 + 保留条数"改为"单次调用 + 抛异常"，重试编排上移到 `AgentLoop.compressWithRetry`（复用同一策略、可中断、耗尽中止本轮）。

**Tech Stack:** JDK 8、Maven、JUnit 4（surefire）、okhttp + MockWebServer、JavaFX 8。

## Global Constraints

- JDK 8 兼容（无 var/无 List.of/无 String.isBlank）；Maven 单模块；资源目录 `src/resource`
- 构建/测试命令：`mvn compile`、`mvn test`（必须全绿）；单测运行 `mvn test -Dtest=<类名>`
- 中文注释与 commit（conventional 格式）
- 核心规约（docs/CONVENTIONS.md）：core 内不新增循环依赖；错误处理 LLM 错误抛 `LlmException`；API 契约（reasoning_content 原样回传、tool_call↔tool 消息完整配对）
- 阈值/间隔/摘要上限全部硬编码，**不进** `config.properties`、**不进** model.json
- 文档设计源：`docs/superpowers/specs/2026-09-13-重试分类与压缩简化-design.md`
- 历史 spec/plan 文档中的旧数值（20 分钟等）**不回改**

## 文件结构（改动落点）

| 文件 | 职责 / 本次改动 |
|---|---|
| `src/main/java/com/minion/core/llm/LlmException.java` | 新增 `Type.EMPTY_RESPONSE` |
| `src/main/java/com/minion/core/llm/DeepSeekClient.java` | 空响应改 `EMPTY_RESPONSE/retryable=true` |
| `src/main/java/com/minion/core/agent/RetryPolicy.java` | 重写：Kind 分类间隔、12 分钟、isTransient/kindOf |
| `src/main/java/com/minion/core/agent/RetryProgress.java` | 新增 `nextDelayMs`（下次等待时长） |
| `src/main/java/com/minion/gui/RunningIndicator.java` | 重试文案追加"约 N 秒后重试" |
| `src/main/java/com/minion/core/agent/AgentLoop.java` | 重试循环接入分类；`compactNow`/自动压缩走 `compressWithRetry` |
| `src/main/java/com/minion/core/agent/SubAgentLoop.java` | 重试循环接入分类（与主循环一致） |
| `src/main/java/com/minion/core/context/ContextManager.java` | 重写：单次压缩、抛异常、写死 0.65/0.8/5000 字 |
| `src/main/java/com/minion/core/config/ModelConfig.java` | 删 `compressThreshold`/`keepRecentMessages` |
| `src/main/java/com/minion/core/config/ModelManager.java` | 删两字段读写；内置默认 200k + 名字 deepseek/qwen |
| `src/main/java/com/minion/gui/dialog/SettingsDialog.java` | 模型表单删两行输入；新建默认 200000 |
| `src/main/java/com/minion/gui/session/SessionManager.java` | ContextManager 新构造/update 签名 |
| `src/main/java/com/minion/gui/input/InputView.java` | 删 Esc 终止分支与文案 |
| `src/resource/config_deepseek.properties`、`config_qwen.properties` | 删无代码读取的 `context.compressThreshold/keepRecentMessages` 行 |
| `src/test/java/.../RetryPolicyTest.java`、`RetryProgressTest.java`、`ContextManagerTest.java`、`AgentLoopTest.java`、`SubAgentLoopTest.java`、`AgentLoopCompactTest.java`、`ModelManagerTest.java`、`RunningIndicatorTest.java`、`DeepSeekClientTest.java` | 按任务同步 |
| `docs/ARCHITECTURE.md`、`README.md` | 压缩与重试描述、Esc 说明同步 |

---

### Task 1: LlmException 新增 EMPTY_RESPONSE + DeepSeekClient 空响应可重试

**Files:**
- Modify: `src/main/java/com/minion/core/llm/LlmException.java:6`
- Modify: `src/main/java/com/minion/core/llm/DeepSeekClient.java:171`
- Test: `src/test/java/com/minion/core/llm/DeepSeekClientTest.java`（文件末尾新增用例）

**Interfaces:**
- Consumes: 无
- Produces: `LlmException.Type.EMPTY_RESPONSE`（后续 Task 2 的 `isTransient/kindOf`、Task 5 的 `ContextManager` 空摘要均引用）

- [x] **Step 1: 写失败测试**

在 `DeepSeekClientTest` 末尾（第 533 行 `}` 之前）新增：

```java
    /** HTTP 200 之外的成功码但无响应体（204 无 body）→ EMPTY_RESPONSE 且可重试（按 500 类） */
    @Test
    public void emptyResponseBody_isRetryableEmptyResponse() throws Exception {
        server.enqueue(new MockResponse().setResponseCode(204));
        DeepSeekClient client = new DeepSeekClient(
                server.url("/v1/chat/completions").toString(), "sk-test", "m",
                false, null, "deepseek");
        try {
            client.streamChat(Collections.singletonList(Message.user("hi")), null,
                    new StreamHandler() {
                        @Override
                        public void onFinish(String f, Usage u, List<ToolCall> t) { }
                    });
            fail("应抛 EMPTY_RESPONSE");
        } catch (LlmException e) {
            assertEquals(LlmException.Type.EMPTY_RESPONSE, e.type);
            assertTrue(e.retryable);
        } finally {
            client.close();
        }
    }
```

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -Dtest=DeepSeekClientTest#emptyResponseBody_isRetryableEmptyResponse`
Expected: 编译失败（`EMPTY_RESPONSE` 不存在）

> 执行记录：执行时工作区已有前次会话写入的未提交改动（测试 + 枚举 + 原计划片段），编译通过但用例**运行失败**（未抛异常）——暴露原实现片段在 okhttp 4 下对 204 不成立（见 Step 3 偏差说明）。

- [x] **Step 3: 实现**

`LlmException.java` 第 6 行枚举改为：

```java
    public enum Type { AUTH, RATE_LIMIT, BAD_REQUEST, NETWORK, TIMEOUT, OTHER, EMPTY_RESPONSE }
```

`DeepSeekClient.java` 第 171 行改为（**执行偏差**：原片段 `response.body() == null` 在 okhttp 4.12 下对 204 恒为 false——204 的 body 是长度为 0 的占位而非 null，按 Step 1 测试断言修正为 `contentLength() == 0L`）：

```java
            // 空响应：okhttp 4 对 204 等无 body 响应返回的是"长度为 0 的空 body"（body() 不再为 null），
            // 故按 contentLength()==0 判定；body()==null 分支保留作防御
            if (response.body() == null || response.body().contentLength() == 0L) {
                throw new LlmException(LlmException.Type.EMPTY_RESPONSE, "空响应（服务端未返回内容）", true);
            }
```

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -Dtest=DeepSeekClientTest`
Expected: PASS（全部用例，含新增）

> 执行记录：`DeepSeekClientTest` 24 用例全绿；另跑全量 `mvn test` 1049 用例全绿。

- [x] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/llm/LlmException.java src/main/java/com/minion/core/llm/DeepSeekClient.java src/test/java/com/minion/core/llm/DeepSeekClientTest.java
git commit -m "feat(llm): 空响应新增 EMPTY_RESPONSE 类型并标记可重试"
```

---

### Task 2: RetryPolicy 重写（分类间隔 + 12 分钟 + 判定收敛）

**Files:**
- Modify: `src/main/java/com/minion/core/agent/RetryPolicy.java`（整文件重写）
- Test: `src/test/java/com/minion/core/agent/RetryPolicyTest.java`（整文件重写）

**Interfaces:**
- Consumes: `LlmException.Type.EMPTY_RESPONSE`（Task 1）
- Produces:
  - `enum RetryPolicy.Kind { SHORT, LONG }`
  - `static RetryPolicy RetryPolicy.transientErrors()`（短 5000 / 长 30000 / 总 720000）
  - `RetryPolicy(long shortDelayMs, long longDelayMs, long maxTotalMs)`
  - `long delayMs(Kind kind)`、`boolean isExhausted(long elapsedMs)`
  - `static boolean isTransient(LlmException e)`、`static Kind kindOf(LlmException e)`（Task 4/7 调用）

- [x] **Step 1: 写失败测试（整文件替换 RetryPolicyTest.java）**

```java
package com.minion.core.agent;

import com.minion.core.llm.LlmException;
import org.junit.Test;

import static org.junit.Assert.*;

public class RetryPolicyTest {

    @Test
    public void transientErrors_defaultParams() {
        RetryPolicy p = RetryPolicy.transientErrors();
        assertEquals(5000, p.shortDelayMs);
        assertEquals(30000, p.longDelayMs);
        assertEquals(720000L, p.maxTotalMs); // 12 分钟
    }

    @Test
    public void delayMs_perKind() {
        RetryPolicy p = RetryPolicy.transientErrors();
        assertEquals(5000, p.delayMs(RetryPolicy.Kind.SHORT));
        assertEquals(30000, p.delayMs(RetryPolicy.Kind.LONG));
    }

    @Test
    public void isExhausted_boundary() {
        RetryPolicy p = new RetryPolicy(10, 20, 1000);
        assertFalse(p.isExhausted(999));
        assertTrue(p.isExhausted(1000));
        assertTrue(p.isExhausted(1001));
    }

    /** 可长重试集合：429/超时/可恢复网络/500 类（含 503/504）/空响应；DNS 与 4xx 不放行 */
    @Test
    public void isTransient_coversAllClasses() {
        assertTrue(RetryPolicy.isTransient(LlmException.of(429, null)));
        assertTrue(RetryPolicy.isTransient(new LlmException(LlmException.Type.TIMEOUT, "t", true)));
        assertTrue(RetryPolicy.isTransient(new LlmException(LlmException.Type.NETWORK, "n", true)));
        assertTrue(RetryPolicy.isTransient(LlmException.of(500, "x")));
        assertTrue(RetryPolicy.isTransient(LlmException.of(502, "x")));
        assertTrue(RetryPolicy.isTransient(LlmException.of(503, "x")));
        assertTrue(RetryPolicy.isTransient(LlmException.of(504, "x")));
        assertTrue(RetryPolicy.isTransient(new LlmException(LlmException.Type.EMPTY_RESPONSE, "空响应", true)));
        assertFalse(RetryPolicy.isTransient(new LlmException(LlmException.Type.NETWORK, "dns", false)));
        assertFalse(RetryPolicy.isTransient(LlmException.of(400, "bad")));
        assertFalse(RetryPolicy.isTransient(LlmException.of(401, "auth")));
        assertFalse(RetryPolicy.isTransient(new LlmException(LlmException.Type.BAD_REQUEST, "x", false)));
    }

    /** 类别：429/超时/网络 → SHORT；500 类与空响应 → LONG */
    @Test
    public void kindOf_shortVsLong() {
        assertEquals(RetryPolicy.Kind.SHORT, RetryPolicy.kindOf(LlmException.of(429, null)));
        assertEquals(RetryPolicy.Kind.SHORT,
                RetryPolicy.kindOf(new LlmException(LlmException.Type.TIMEOUT, "t", true)));
        assertEquals(RetryPolicy.Kind.SHORT,
                RetryPolicy.kindOf(new LlmException(LlmException.Type.NETWORK, "n", true)));
        assertEquals(RetryPolicy.Kind.LONG, RetryPolicy.kindOf(LlmException.of(500, "x")));
        assertEquals(RetryPolicy.Kind.LONG, RetryPolicy.kindOf(LlmException.of(503, "x")));
        assertEquals(RetryPolicy.Kind.LONG,
                RetryPolicy.kindOf(new LlmException(LlmException.Type.EMPTY_RESPONSE, "空响应", true)));
    }
}
```

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -Dtest=RetryPolicyTest`
Expected: 编译失败（`shortDelayMs`/`Kind` 不存在）

> 执行记录：编译失败符合预期（`Kind`/`kindOf`/`shortDelayMs` 不存在）。偏差注记：本计划 Task 2 完成后主代码调用点（`AgentLoop:481`/`SubAgentLoop:111` 的 `delayMs(int)`）与 `AgentLoopTest`/`SubAgentLoopTest` 旧构造会令全仓不可编译，Step 4 无法直接运行——执行时一并做了编译适配（见 Step 4 记录）。

- [x] **Step 3: 实现（整文件替换 RetryPolicy.java）**

```java
package com.minion.core.agent;

import com.minion.core.llm.LlmException;

/** 瞬时错误长重试策略：按错误类别区分等待间隔 + 墙钟总时长上限（含分类判定）。
 *  默认：短等待 5s（429 限流 / 超时 / 可恢复网络错误）、长等待 30s（500 类：500/502/503/504
 *  及其他 5xx，与空响应）、墙钟总时长 12 分钟。等待时长由"最近一次失败的类别"决定。
 *  maxTotalMs 的计时口径由调用方决定，本类累计的是"进入重试态以来的真实时间"
 *  （含每次请求自身耗时），非纯等待时长；测试可构造小参数实例覆写。 */
public class RetryPolicy {

    /** 等待类别：SHORT=429/超时/可恢复网络错误；LONG=500 类/空响应 */
    public enum Kind { SHORT, LONG }

    /** 默认瞬时错误策略：SHORT 5s / LONG 30s，墙钟总时长 12 分钟 */
    public static RetryPolicy transientErrors() {
        return new RetryPolicy(5000, 30000, 12 * 60 * 1000L);
    }

    public final long shortDelayMs;
    public final long longDelayMs;
    public final long maxTotalMs;

    public RetryPolicy(long shortDelayMs, long longDelayMs, long maxTotalMs) {
        this.shortDelayMs = shortDelayMs;
        this.longDelayMs = longDelayMs;
        this.maxTotalMs = maxTotalMs;
    }

    /** 按最近一次失败的错误类别取等待时长 */
    public long delayMs(Kind kind) {
        return kind == Kind.LONG ? longDelayMs : shortDelayMs;
    }

    /** 距重试态起点已过去的真实时间是否达到总时长上限（入参由调用方以墙钟计算） */
    public boolean isExhausted(long elapsedMs) {
        return elapsedMs >= maxTotalMs;
    }

    /** 可进长重试的瞬时错误：429 / 超时 / 可恢复网络错误 / 500 类（含 503/504 等其他 5xx）/ 空响应。
     *  DNS 解析失败（NETWORK 且 retryable=false）与 400/401/403 等永久性错误不放行 */
    public static boolean isTransient(LlmException e) {
        if (e.type == LlmException.Type.EMPTY_RESPONSE) return true;
        if (e.type == LlmException.Type.RATE_LIMIT
                || e.type == LlmException.Type.TIMEOUT) return true;
        if (e.type == LlmException.Type.NETWORK) return e.retryable;
        return e.httpCode >= 500;
    }

    /** 错误类别（仅对 isTransient 为真的错误有意义）：500 类与空响应 LONG，其余瞬时错误 SHORT */
    public static Kind kindOf(LlmException e) {
        if (e.type == LlmException.Type.EMPTY_RESPONSE) return Kind.LONG;
        if (e.type == LlmException.Type.RATE_LIMIT
                || e.type == LlmException.Type.TIMEOUT
                || e.type == LlmException.Type.NETWORK) return Kind.SHORT;
        return Kind.LONG; // httpCode >= 500
    }
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -Dtest=RetryPolicyTest`
Expected: PASS（6 个用例）

> 执行记录：`RetryPolicyTest` 5 个测试方法全绿（计划写"6 个用例"为笔误，代码块实为 5 个 `@Test`）。为使全仓可编译，同步做了 Task 4 的前置编译适配：主/子循环等待时长改为 `retryPolicy.delayMs(RetryPolicy.kindOf(last))`（分类间隔生效；503/504 判定切换、指示器等待时长、判定收敛仍待 Task 4），`AgentLoopTest`/`SubAgentLoopTest` 的重试策略构造按 Task 4 对照表换 3 参。全量 `mvn test` 1050 用例全绿，`mvn compile` 成功。

- [x] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/agent/RetryPolicy.java src/test/java/com/minion/core/agent/RetryPolicyTest.java
git commit -m "refactor(agent): RetryPolicy 改为分类间隔（5s/30s）与 12 分钟墙钟，判定收敛为 isTransient/kindOf"
```

> 执行记录：提交 `ee6ff85`，实际含 6 个文件（Task 2 两文件 + 编译适配的 `AgentLoop`/`SubAgentLoop`/`AgentLoopTest`/`SubAgentLoopTest`，使每个提交点均可编译、`mvn test` 全绿）。判定收敛（两处私有 `isTransientError` 替换为 `RetryPolicy.isTransient`、删除私有方法）与 503/504 纳入长重试仍属 Task 4。

---

### Task 3: RetryProgress 携带等待时长 + 指示器文案

**Files:**
- Modify: `src/main/java/com/minion/core/agent/RetryProgress.java`
- Modify: `src/main/java/com/minion/gui/RunningIndicator.java:74-80`（retryText 与新增 delayPart）
- Test: `src/test/java/com/minion/gui/RunningIndicatorTest.java`（新增用例）

**Interfaces:**
- Consumes: 无（`LlmException`）
- Produces: `RetryProgress.from(int attempt, LlmException e, long nextDelayMs)`、字段 `final long nextDelayMs`；`RunningIndicator.delayPart(RetryProgress)`（Task 4/7 使用三参 from）

- [x] **Step 1: 写失败测试**

先把既有用例 `retryText_unknownCode_defensive` 改为（503/504 已纳入长重试，"理论不可达"不再成立）：

```java
    /** 未知错误码防御性显示；503/504 已纳入 500 类长重试 */
    @Test
    public void retryText_unknownCode_defensive() {
        assertEquals("503服务不可用", RunningIndicator.codeLabel(503));
        assertEquals("504网关超时", RunningIndicator.codeLabel(504));
        assertEquals("HTTP 599", RunningIndicator.codeLabel(599));
    }
```

再在 `RunningIndicatorTest` 末尾新增（其余既有断言不变——旧工厂 `nextDelayMs=0` 不追加后缀，零回归）：

```java
    /** 带下次等待时长：文案追加「约 N 秒后重试」（毫秒向上取整到秒） */
    @Test
    public void retryText_withDelay_appendsWaitHint() {
        RetryProgress p = RetryProgress.from(2, LlmException.of(500, "{\"error\":\"boom\"}"), 30000);
        assertEquals("正在加载中...(500服务报错，重试第2次{\"error\":\"boom\"}，约 30 秒后重试)",
                RunningIndicator.retryText(p, "正在加载中..."));
        RetryProgress q = RetryProgress.from(1,
                new LlmException(LlmException.Type.RATE_LIMIT, "429", true), 5000);
        assertEquals("可随时补充信息...(429限流，重试第1次，约 5 秒后重试)",
                RunningIndicator.retryText(q, "可随时补充信息..."));
    }

    /** 不足 1 秒向上取整到 1 秒；无等待时长（<=0）不追加 */
    @Test
    public void delayPart_roundsUpAndSkipsZero() {
        assertEquals("，约 1 秒后重试",
                RunningIndicator.delayPart(RetryProgress.from(1, LlmException.of(429, null), 1)));
        assertEquals("", RunningIndicator.delayPart(RetryProgress.of(1, 429, null)));
    }

    /** 压缩期间的重试：基础文案取压缩固定文案（setRetryProgress 内 retryBase 选择），等待时长可见 */
    @Test
    public void retryText_duringCompressing_usesCompressingBase() {
        RetryProgress p = RetryProgress.from(1,
                new LlmException(LlmException.Type.EMPTY_RESPONSE, "空响应", true), 30000);
        assertEquals("上下文压缩中...(空响应，重试第1次，约 30 秒后重试)",
                RunningIndicator.retryText(p, RunningIndicator.COMPRESSING_TEXT));
    }
```

文件头需补 `import com.minion.core.llm.LlmException;`（若无）。

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -Dtest=RunningIndicatorTest`
Expected: 编译失败（三参 `from` / `delayPart` 不存在）

> 执行记录：编译失败符合预期（`RetryProgress.from(int, LlmException, long)`、`RunningIndicator.delayPart` 不存在）。同时补了测试文件缺失的 `import com.minion.core.llm.LlmException;`。

- [x] **Step 3: 实现**

`RetryProgress.java`（整文件替换，避免重载歧义）：

```java
package com.minion.core.agent;

import com.minion.core.llm.LlmException;

/** 重试进度（agent 循环 → UI 指示器）：attempt=0 表示退出重试态（复位），
 *  httpCode/body 为最近一次失败的 HTTP 状态码与响应体（429/500/502 等完整展示）。
 *  网络超时/网络错误/空响应无 HTTP 码，改用 label 携带中文标签、body 携带异常消息。 */
public final class RetryProgress {

    public final int attempt;
    public final int httpCode;
    public final String body;
    /** 非 HTTP 类错误的中文标签（网络超时/网络错误/空响应）；null 表示 HTTP 错误，标签由 httpCode 推导 */
    public final String label;
    /** 距下次重试的等待毫秒（0=不展示等待提示） */
    public final long nextDelayMs;

    private RetryProgress(int attempt, int httpCode, String body, String label, long nextDelayMs) {
        this.attempt = attempt;
        this.httpCode = httpCode;
        this.body = body;
        this.label = label;
        this.nextDelayMs = nextDelayMs;
    }

    public static RetryProgress of(int attempt, int httpCode, String body) {
        return new RetryProgress(attempt, httpCode, body, null, 0);
    }

    /** 网络类错误进度（httpCode 恒 0，detail 为异常消息） */
    public static RetryProgress ofNetwork(int attempt, String label, String detail) {
        return new RetryProgress(attempt, 0, detail, label, 0);
    }

    /** 异常 → 进度映射（不带等待时长；展示层的拼接/截断与此无关） */
    public static RetryProgress from(int attempt, LlmException e) {
        return from(attempt, e, 0);
    }

    /** 异常 → 进度映射 + 下次等待时长（指示器展示「约 N 秒后重试」） */
    public static RetryProgress from(int attempt, LlmException e, long nextDelayMs) {
        RetryProgress base = classify(attempt, e);
        return new RetryProgress(base.attempt, base.httpCode, base.body, base.label, nextDelayMs);
    }

    /** 归类：超时/网络/空响应 → 中文标签；其余 → HTTP 状态码 */
    private static RetryProgress classify(int attempt, LlmException e) {
        if (e.type == LlmException.Type.TIMEOUT) {
            return ofNetwork(attempt, "网络超时", e.getMessage());
        }
        if (e.type == LlmException.Type.NETWORK) {
            return ofNetwork(attempt, "网络错误", e.getMessage());
        }
        if (e.type == LlmException.Type.EMPTY_RESPONSE) {
            return ofNetwork(attempt, "空响应", e.getMessage());
        }
        return of(attempt, e.httpCode, e.body);
    }

    /** 重试超时总结文案的错误前缀：429/500 类用状态码，网络类/空响应用中文标签 */
    public static String tag(LlmException e) {
        if (e.type == LlmException.Type.TIMEOUT) return "网络超时";
        if (e.type == LlmException.Type.NETWORK) return "网络错误";
        if (e.type == LlmException.Type.EMPTY_RESPONSE) return "空响应";
        if (e.type == LlmException.Type.RATE_LIMIT) return "429";
        return String.valueOf(e.httpCode);
    }

    /** 退出重试态（复位指示器） */
    public static RetryProgress none() {
        return new RetryProgress(0, 0, null, null, 0);
    }
}
```

`RetryProgressTest` 增补 EMPTY_RESPONSE 归类用例（既有断言全部不变）：

```java
    /** 空响应：中文标签 + tag 前缀（LONG 类瞬时错误） */
    @Test
    public void from_emptyResponse_mapsToLabel() {
        LlmException e = new LlmException(LlmException.Type.EMPTY_RESPONSE, "空响应（服务端未返回内容）", true);
        assertEquals("空响应", RetryProgress.from(2, e).label);
        assertEquals("空响应", RetryProgress.tag(e));
        assertEquals(0, RetryProgress.from(2, e).httpCode);
    }
```

`RunningIndicator.java`：

```java
    /** 重试文案：冻结基础文案 + 错误标签/次数/详情 + 等待时长后缀 */
    static String retryText(RetryProgress p, String base) {
        return base + "(" + labelOf(p) + "，重试第" + p.attempt + "次" + bodyPart(p) + delayPart(p) + ")";
    }

    /** 等待时长后缀：如「，约 30 秒后重试」；nextDelayMs<=0 时为空（毫秒向上取整到秒） */
    static String delayPart(RetryProgress p) {
        if (p.nextDelayMs <= 0) return "";
        return "，约 " + ((p.nextDelayMs + 999) / 1000) + " 秒后重试";
    }
```

`codeLabel` 增加防御显示 503/504（长重试后可能到达展示层）：

```java
        if (httpCode == 503) return "503服务不可用";
        if (httpCode == 504) return "504网关超时";
```

`setRetryProgress` 与 `setCompressing` 各改一处，保证"压缩期间的重试"文案以压缩固定文案为基（挂载状态机不变）：

```java
    public void setRetryProgress(RetryProgress p) {
        if (!running) return;
        retryProgress = p;
        if (p.attempt >= 1) {
            if (retryBase == null) {
                // 压缩中的重试：基础文案取压缩固定文案（否则显示成普通加载文案，用户不知在压缩）
                retryBase = compressing ? COMPRESSING_TEXT : pickText(rnd);
                if (rotateText != null) rotateText.stop();
            }
            text.setText(retryText(p, retryBase));
        } else {
            retryBase = null;
            text.setText(displayText(compressing, pickText(rnd)));
            startRotateText();
        }
    }
```

```java
    public void setCompressing(boolean compressing) {
        this.compressing = compressing;   // 重试态也更新字段：重试结束后能回到正确文案
        if (retryBase != null) return;    // 重试态：不重绘（重试文案优先）
        if (!running) return;
        text.setText(displayText(compressing, pickText(rnd)));
        if (compressing) {
            if (rotateText != null) rotateText.stop();
        } else {
            startRotateText();
        }
    }
```

（验收效果：压缩重试期间显示「上下文压缩中...(空响应，重试第1次，约 30 秒后重试)」。）

> 执行记录：实现按上述片段落地，另有两处必要偏差（使本任务自带用例可过，均为防御性小改）：
> 1. `RetryProgress.classify` 增 RATE_LIMIT 且 `httpCode<=0` → 按 429 归一化：用例用 `new LlmException(Type.RATE_LIMIT, "429", true)`（三参构造 httpCode=0）构造，原片段会渲染成「HTTP 0」；归一化与既有 `tag()` 对 RATE_LIMIT 的处理一致，真实 429 异常恒带 httpCode，不受影响。
> 2. `RunningIndicator.bodyPart` 增「详情与标签完全相同 → 不重复展示」：用例的 EMPTY_RESPONSE 异常消息恰为"空响应"，原片段会渲染成「空响应，重试第1次：空响应」（计划期望为不展示）；该规则与既有"剥重复前缀"规则同旨，真实消息（"空响应（服务端未返回内容）"/"压缩模型返回空摘要"）仍照常展示。

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -Dtest=RunningIndicatorTest -Dtest=RetryProgressTest`
Expected: PASS（新增 2 条 + 既有全部；既有文案断言不因 `nextDelayMs=0` 变化）

> 执行记录：`RunningIndicatorTest` 16 用例、`RetryProgressTest` 9 用例全绿（新增 3+1 条）；全量 `mvn test` 1054 用例全绿（较 Task 2 基线 +4），`mvn compile` 成功。首轮跑测暴露的 2 个失败（`HTTP 0`、`空响应：空响应`）由 Step 3 记录的两处小改修正，计划测试全部按原文通过。

- [x] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/agent/RetryProgress.java src/main/java/com/minion/gui/RunningIndicator.java src/test/java/com/minion/gui/RunningIndicatorTest.java
git commit -m "feat(gui): 重试指示器展示下次等待时长（约 N 秒后重试）"
```

> 执行记录：提交 `aa9e5d4`，实际含 4 个文件（计划 `git add` 漏列了同步增补 EMPTY_RESPONSE 归类用例的 `RetryProgressTest.java`）。

---

### Task 4: 主循环与子 agent 接入分类间隔（含 503/504 长重试与测试同步）

**Files:**
- Modify: `src/main/java/com/minion/core/agent/AgentLoop.java`（重试循环 ~469-505、删除 isTransientError ~634-642、注释）
- Modify: `src/main/java/com/minion/core/agent/SubAgentLoop.java`（重试循环 ~99-140、删除 isTransientError ~196-204）
- Test: `src/test/java/com/minion/core/agent/AgentLoopTest.java`、`SubAgentLoopTest.java`

**Interfaces:**
- Consumes: `RetryPolicy.transientErrors()/delayMs(Kind)/isTransient/kindOf`（Task 2）、`RetryProgress.from(int, LlmException, long)`（Task 3）
- Produces: 无（行为变更：503/504 等 5xx 进长重试；等待分类；12 分钟墙钟）

- [x] **Step 1: 改测试（先失败）**

> 执行状态（Task 2 于 145f255 提前完成）：以下对照表的构造替换**已全部完成**，保留作对照；剩余改动为后面的两个用例增改与一处更名。
>
> Task 4 执行记录：五个用例改动已全部完成——`serverError503_notLongRetried` 替换为 `serverError503_longRetried`、新增 `retry_delayFollowsLatestErrorKind`（AgentLoopTest）、`partialOutputThen503_noFastRetry` 更名 `partialOutputThen503_gateBlocksLongRetry`（断言不变）、SubAgentLoopTest 新增 `subAgent_serverError503_longRetried`、AgentLoopTest:560 注释 20→12 分钟。**偏差**：计划稿的断言 `assertEquals(Long.valueOf(3), ...get(0).nextDelayMs)` 对 `long` 字段构成 `assertEquals(Long,long)` 重载歧义（编译失败），改为 `assertEquals(3L, ...)` / `assertEquals(7L, ...)`。

`AgentLoopTest` 中 `new RetryPolicy(...)` 全部替换（对照表）：

| 旧 | 新 |
|---|---|
| `new RetryPolicy(10, 0, 10, 100)` | `new RetryPolicy(10, 10, 100)` |
| `new RetryPolicy(10, 0, 10, 60000)` | `new RetryPolicy(10, 10, 60000)` |
| `new RetryPolicy(10, 0, 10, 50)` | `new RetryPolicy(10, 10, 50)` |
| `new RetryPolicy(10, 10, 100, 60000)` | `new RetryPolicy(10, 10, 60000)` |
| `new RetryPolicy(10, 10, 20, 50)` | `new RetryPolicy(10, 20, 50)` |
| `new RetryPolicy(60000, 60000, 60000, 3600000L)` | `new RetryPolicy(60000, 60000, 3600000L)` |

`SubAgentLoopTest` 同样替换（`(10,10,100,60000)`→`(10,10,60000)`；`(10,10,20,50)`→`(10,20,50)`；`(10,10,20,60000)`→`(10,20,60000)`；`(10,0,10,60000)`→`(10,10,60000)`；`(10,0,10,50)`→`(10,10,50)`）。

`AgentLoopTest` 中把 `serverError503_notLongRetried` 整个用例替换为：

```java
    /** 503 服务不可用：纳入 500 类长重试（30s 类，测试小参数），成功后静默恢复 */
    @Test
    public void serverError503_longRetried() {
        llm.addTurnThrow(LlmException.of(503, "unavailable"));
        llm.addTurn("最终回复");
        AgentLoop loop = newLoop();
        loop.retryPolicy = new RetryPolicy(10, 10, 60000);
        loop.runUserTurn("任务");
        assertEquals(2, llm.requests.size()); // 原始请求 + 1 次长重试（不再走 0.5s 快速重试）
        assertTrue(ui.warnings.isEmpty());     // 无「自动重试 1 次」
        assertTrue(ui.errors.isEmpty());
        assertEquals(Arrays.asList(1, 0), ui.retryAttempts());
    }
```

把 `partialOutputThen503_noFastRetry` 更名为 `partialOutputThen503_gateBlocksLongRetry`（断言不变：1 次请求、无 warning）。

新增分类间隔用例（`AgentLoopTest`）：

```java
    /** 等待时长按最近一次错误类别：429 → 短等待，500 → 长等待 */
    @Test
    public void retry_delayFollowsLatestErrorKind() {
        llm.addTurnThrow(LlmException.of(429, null));
        llm.addTurnThrow(LlmException.of(500, "{\"error\":\"boom\"}"));
        llm.addTurn("最终回复");
        AgentLoop loop = newLoop();
        loop.retryPolicy = new RetryPolicy(3, 7, 60000);
        loop.runUserTurn("任务");
        assertEquals(Long.valueOf(3), ui.retryProgress.get(0).nextDelayMs); // 429 → SHORT
        assertEquals(Long.valueOf(7), ui.retryProgress.get(1).nextDelayMs); // 500 → LONG
    }
```

`SubAgentLoopTest` 新增对应用例（对称，类名 `subAgent_serverError503_longRetried`，断言 `assertEquals(2, llm.requests.size())`）。

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -Dtest=AgentLoopTest,SubAgentLoopTest`
Expected: 失败——`serverError503_longRetried` 断言 `requests.size()==2`，但实现前 503 不属 `isTransientError`（httpCode 500/502 之外）且走短兜底"自动重试 1 次"；`retry_delayFollowsLatestErrorKind` 断言 nextDelayMs 3/7，但指示器仍是二参 `from`（nextDelayMs=0）

> 执行记录：与预期一致，3 处失败——AgentLoopTest 67 用例中 `serverError503_longRetried`（失败于 `assertTrue(ui.warnings.isEmpty())`：503 走短兜底发「自动重试 1 次」；requests.size()==2 与长重试巧合相同）、`retry_delayFollowsLatestErrorKind`（expected:<3> but was:<0>）；SubAgentLoopTest 19 用例中 `subAgent_serverError503_longRetried`（同为警告断言失败）。首次编译另有断言重载歧义（见 Step 1 偏差）。

- [x] **Step 3: 实现（主循环与子 agent 重试循环）**

`AgentLoop.java` 重试循环（Task 2 后的现状，两行顺序已固定为 delay 在前）：

```java
                            attempts++;
                            ui.onRetryProgress(RetryProgress.from(attempts, last)); // 尝试前立即更新指示器
                            long delay = retryPolicy.delayMs(RetryPolicy.kindOf(last));
```

替换为：

```java
                            attempts++;
                            long delay = retryPolicy.delayMs(RetryPolicy.kindOf(last));
                            ui.onRetryProgress(RetryProgress.from(attempts, last, delay)); // 尝试前更新指示器（含等待时长）
```

并把判定 `if (isTransientError(e) && noOutputYet(content, thinking))` 改为
`if (RetryPolicy.isTransient(e) && noOutputYet(content, thinking))`，循环内 `if (!isTransientError(re) || ...)` 改为 `if (!RetryPolicy.isTransient(re) || ...)`；删除私有方法 `isTransientError`。

字段注释改为：

```java
    /** 瞬时错误长重试策略（429/超时/网络 5s；500 类/空响应 30s；墙钟总时长 12 分钟；测试可覆写小参数） */
    public RetryPolicy retryPolicy = RetryPolicy.transientErrors();
```

循环内注释同步：“瞬时错误长重试（内网模型资源差）：按最近一次错误类别等待（429/超时 5s、500 类/空响应 30s），墙钟总时长 12 分钟（RetryPolicy.transientErrors）；覆盖 429/超时/可恢复网络错误/500 类（含 503/504）/空响应”。

短重试兜底分支注释改为：

```java
                    } else if (e.retryable && retries < 1 && noOutputYet(content, thinking)) {
                        // 兜底：可重试但未归类错误（现主流错误均已被长重试覆盖，此分支实际不可达）
                        retries++;
```

`SubAgentLoop.java` 做同样四处变更（判定、delay 计算、指示器三参、删除 isTransientError）；类字段注释同步为 12 分钟与新分类。

另：`AgentLoopTest.java:560` 注释「避免用例跑 20 分钟」改为「避免用例跑 12 分钟」（重试墙钟已改 12 分钟）。

> 执行记录：已按上述完成。**偏差**：实际代码中两行顺序本就是 delay 在前、指示器在后（Task 2 落码时如此），最终形态为
> ```java
>                             long delay = retryPolicy.delayMs(RetryPolicy.kindOf(last));
>                             ui.onRetryProgress(RetryProgress.from(attempts, last, delay)); // 尝试前更新指示器（含等待时长）
> ```
> 与计划最终稿一致，无需交换。字段注释与两处判定（`RetryPolicy.isTransient`）同步改毕，两文件私有 `isTransientError` 已删；`LlmException` import 仍被其余代码使用，保留。

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -Dtest=AgentLoopTest,SubAgentLoopTest,RetryPolicyTest,RetryProgressTest`
Expected: PASS

> 执行记录：四类测试全绿（-q 静默无输出）；另跑全量 `mvn test` 1056 用例全绿。

- [x] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/agent/AgentLoop.java src/main/java/com/minion/core/agent/SubAgentLoop.java src/test/java/com/minion/core/agent/AgentLoopTest.java src/test/java/com/minion/core/agent/SubAgentLoopTest.java
git commit -m "feat(agent): 主循环与子 agent 重试改为分类间隔，503/504 等 5xx 纳入长重试"
```

---

### Task 5: ContextManager 新算法（去递归、抛异常、硬编码阈值）

**Files:**
- Modify: `src/main/java/com/minion/core/context/ContextManager.java`（整文件重写）
- Modify: `src/main/java/com/minion/gui/session/SessionManager.java:222-225, 398-401, 499-500`
- Test: `src/test/java/com/minion/core/context/ContextManagerTest.java`（整文件重写）
- Test: `src/test/java/com/minion/core/agent/AgentLoopCompactTest.java`（构造调用点）

**Interfaces:**
- Consumes: `LlmException.Type.EMPTY_RESPONSE`（Task 1）
- Produces:
  - `ContextManager(int maxContextTokens, LlmClient llm, int systemTokens)`
  - `void update(int maxContextTokens)`、`double threshold()`（恒 0.65）
  - `List<Message> compress(List<Message> messages) throws LlmException`（返回入参同引用 = 无可压缩）
  - 常量 `SUMMARY_MAX_CHARS=5000`、`THRESHOLD=0.65`、`COMPRESS_RATIO=0.8`

- [x] **Step 1: 重写测试（先失败）**

`ContextManagerTest` 整文件重写为下列内容（保留：链切块 3 例、pinned 1 例、estimate 1 例；删除全部递归/保留区/二分降级用例）：

> 执行记录：`ContextManagerTest` 13 用例整文件落地；`AgentLoopCompactTest` 6 处构造改 `(A, llm, 0)` 并各加 `loop.retryPolicy = new RetryPolicy(10, 10, 60000)`。**偏差**：`compress_keepRegionHeadKeepsToolPairing` 用例数据修正——连续 `[user, assistant(tools), tool]` 无链结束符，8 轮实为 1 条链（`take=8` 全压、无保留区），每链补一条无工具 `assistant` 收链（4 条/链 ≈39 token），断言不变，回归意图保持。

```java
package com.minion.core.context;

import com.minion.core.llm.FakeLlmClient;
import com.minion.core.llm.LlmException;
import com.minion.core.llm.Message;
import com.minion.core.llm.ToolCall;
import org.junit.Test;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

import static org.junit.Assert.*;

public class ContextManagerTest {

    private static Message assistantWithTools(String... names) {
        Message m = Message.assistant(null);
        List<ToolCall> tcs = new ArrayList<ToolCall>();
        for (String n : names) {
            ToolCall tc = new ToolCall();
            tc.id = "c_" + n;
            tc.name = n;
            tc.arguments = "{}";
            tcs.add(tc);
        }
        m.toolCalls = tcs;
        return m;
    }

    /** 每链 2 条（10 中文字 = 7 token/条、+4 开销 → 22 token/链），供 take 计算断言 */
    private static List<Message> chains(int chainCount, String prefix) {
        List<Message> msgs = new ArrayList<Message>();
        String base = "一二三四五六七八九十";
        for (int i = 0; i < chainCount; i++) {
            msgs.add(Message.user(prefix + i));
            msgs.add(Message.assistant(base));
        }
        return msgs;
    }

    private static List<Message> sampleHistory() {
        List<Message> msgs = new ArrayList<Message>();
        msgs.add(Message.user("任务1"));
        msgs.add(assistantWithTools("Read"));
        msgs.add(Message.toolResult("c_Read", "Read", "内容1"));
        msgs.add(Message.assistant("任务1完成"));
        msgs.add(Message.user("任务2"));
        msgs.add(Message.assistant("直接完成"));
        return msgs;
    }

    @Test
    public void chunkChains_keepsToolPairingIntact() {
        List<List<Message>> chains = ContextManager.chunkChains(sampleHistory());
        assertEquals(2, chains.size());
        assertEquals(4, chains.get(0).size());
        assertEquals(2, chains.get(1).size());
    }

    @Test
    public void chunkChains_skipsSummaryPinnedAndSystem() {
        List<Message> msgs = new ArrayList<Message>();
        msgs.add(Message.system("sys"));
        Message summary = Message.user("【摘要】旧");
        summary.summary = true;
        msgs.add(summary);
        msgs.add(Message.skill("<skill name=\"r\">正文</skill>"));
        msgs.add(Message.user("问题"));
        msgs.add(Message.assistant("回答"));
        List<List<Message>> chains = ContextManager.chunkChains(msgs);
        assertEquals(1, chains.size());
        assertEquals(2, chains.get(0).size());
    }

    @Test
    public void hardcoded_metrics() {
        ContextManager cm = new ContextManager(100, new FakeLlmClient(), 0);
        assertEquals(0.65, cm.threshold(), 1e-9);
        assertEquals(5000, ContextManager.SUMMARY_MAX_CHARS);
        assertTrue(cm.shouldCompress(chains(5, "问题")));   // 5 链 > 100×0.65
        assertFalse(cm.shouldCompress(chains(2, "问题")));  // 2 链 < 65
    }

    /** systemTokens 计入 estimate（差值断言，不依赖具体 token 精度） */
    @Test
    public void estimate_includesSystemTokens() {
        ContextManager cm0 = new ContextManager(100, new FakeLlmClient(), 0);
        ContextManager cm50 = new ContextManager(100, new FakeLlmClient(), 50);
        assertEquals(50, cm50.estimate(chains(2, "问题")) - cm0.estimate(chains(2, "问题")));
    }

    /** take 累加：预算 = 100×0.65×0.8 = 52；从最早链累加首次 ≥ 预算即停（压缩量 ≥ 预算），
     *  后续链整链保留（不切断配对） */
    @Test
    public void compress_takesEarliestChainsUntilCompressBudget() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(100, llm, 0);
        List<Message> input = chains(5, "问题");
        long total = TokenCounter.estimateMessages(input);
        assertTrue("场景前提：总 token 超过压缩预算", total > 52);
        List<Message> result = cm.compress(input);
        assertTrue(result.get(0).summary);
        assertTrue("应保留部分链", result.size() > 1 && result.size() < input.size());
        assertEquals("保留区为完整链（user+assistant 成对）", 0, (result.size() - 1) % 2);
        long kept = TokenCounter.estimateMessages(result.subList(1, result.size()));
        assertTrue("被压部分 token ≥ 预算（52）", total - kept >= 52);
        String batch = llm.completeChatRequests.get(0);
        assertTrue("最早链已压缩", batch.contains("[USER] 问题0"));
        assertFalse("最后一条链保留（不在压缩批次）", batch.contains("[USER] 问题4"));
    }

    /** 全部链合计仍不足压缩预算 → 全压（保留区为空，仅摘要） */
    @Test
    public void compress_allChainsWhenBudgetNotReached() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(10000, llm, 0); // 预算 5200 >> 全部链
        List<Message> result = cm.compress(chains(4, "问题"));
        assertEquals(1, result.size());
        assertTrue(result.get(0).summary);
    }

    /** system/pinned 原样保留在摘要先后，且不进压缩批次 */
    @Test
    public void compress_keepsSystemAndPinned() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(100, llm, 0);
        List<Message> msgs = new ArrayList<Message>();
        msgs.add(Message.system("你是 minion"));
        for (int i = 0; i < 5; i++) {
            msgs.add(Message.user("问题" + i));
            if (i == 2) msgs.add(Message.skill("<skill>正文</skill>"));
            msgs.add(Message.assistant("一二三四五六七八九十"));
        }
        List<Message> result = cm.compress(msgs);
        assertEquals(Message.Role.SYSTEM, result.get(0).role);
        assertTrue(result.get(1).summary);
        assertTrue(result.get(2).pinned);
        assertFalse(llm.completeChatRequests.get(0).contains("<skill>正文</skill>"));
    }

    /** 二次压缩：旧摘要并入输入置前，新摘要取代旧摘要（不丢信息） */
    @Test
    public void compress_secondRun_feedsOldSummaryBack() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】第一轮";
        ContextManager cm = new ContextManager(100, llm, 0);
        List<Message> first = cm.compress(chains(5, "问题"));
        assertTrue(first.get(0).summary);
        // 再追加消息后二次压缩
        List<Message> second = new ArrayList<Message>(first);
        for (int i = 0; i < 4; i++) {
            second.add(Message.user("新" + i));
            second.add(Message.assistant("一二三四五六七八九十"));
        }
        cm.compress(second);
        assertTrue(llm.completeChatRequests.get(1).contains("【摘要】第一轮"));
    }

    /** 无可压缩（无链）→ 返回同一引用，不抛错 */
    @Test
    public void compress_noChains_returnsSameInstance() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        ContextManager cm = new ContextManager(100, llm, 0);
        List<Message> msgs = new ArrayList<Message>();
        msgs.add(Message.system("sys"));
        msgs.add(Message.skill("<skill>正文</skill>"));
        assertSame(msgs, cm.compress(msgs));
    }

    /** 压缩请求抛异常 → 原样上抛（不再降级/静默返回） */
    @Test
    public void compress_llmThrows_propagates() {
        FakeLlmClient llm = new FakeLlmClient();
        llm.throwOnCompleteChat = true;
        ContextManager cm = new ContextManager(100, llm, 0);
        try {
            cm.compress(chains(5, "问题"));
            fail("应抛 LlmException");
        } catch (LlmException e) {
            assertEquals(LlmException.Type.OTHER, e.type);
        }
    }

    /** 空摘要 → EMPTY_RESPONSE（可重试，按 500 类） */
    @Test
    public void compress_emptySummary_throwsEmptyResponse() {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "  ";
        ContextManager cm = new ContextManager(100, llm, 0);
        try {
            cm.compress(chains(5, "问题"));
            fail("应抛 EMPTY_RESPONSE");
        } catch (LlmException e) {
            assertEquals(LlmException.Type.EMPTY_RESPONSE, e.type);
            assertTrue(e.retryable);
        }
    }

    /** 保留区首条不为孤立 TOOL：链内配对（assistant(tools)+tool）不被切开 */
    @Test
    public void compress_keepRegionHeadKeepsToolPairing() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(200, llm, 0); // 触发 130、预算 104（8 链 ~26 token/链 → 部分保留）
        List<Message> msgs = new ArrayList<Message>();
        for (int i = 0; i < 8; i++) { // 每链 3 条 = 26 token
            msgs.add(Message.user("一二三四五六七八九十"));
            msgs.add(assistantWithTools("Read"));
            msgs.add(Message.toolResult("c_Read", "Read", "一二三四五六七八九十"));
        }
        List<Message> result = cm.compress(msgs);
        assertTrue(result.get(0).summary);
        assertTrue("应保留部分链", result.size() > 1);
        assertNotEquals(Message.Role.TOOL, result.get(1).role); // 保留区首条为链首（user）
        assertEquals(Message.Role.TOOL, result.get(3).role);    // 配对完整：assistant(tools) 后紧跟 tool
    }

    @Test
    public void update_changesMaxTokens() {
        ContextManager cm = new ContextManager(1000, new FakeLlmClient(), 10);
        cm.update(100);
        assertEquals(100, cm.maxTokens());
    }
}
```

`AgentLoopCompactTest`：把 6 处 `new ContextManager(A, 0.8, B, llm, 0)` 改为 `new ContextManager(A, llm, 0)`（A=50 时触发阈值变 32.5，注释里 `50×0.8=40` 换成 `50×0.65=32.5`）。同时每个用例开头加：

```java
        loop.retryPolicy = new RetryPolicy(10, 10, 60000); // 压缩失败重试的小参数（防真等）
```

（`AgentLoopCompactTest` 需 `import com.minion.core.agent.RetryPolicy;` 同包可省略 import。）

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -Dtest=ContextManagerTest,AgentLoopCompactTest`
Expected: 编译失败（构造签名/`lastCompressAttempted` 不存在）

> 执行记录：与预期一致——`ContextManagerTest` 报构造签名不匹配（14 处）、`SUMMARY_MAX_CHARS`/`update(int)` 找不到符号；`AgentLoopCompactTest` 5 处构造签名不匹配。

- [x] **Step 3: 实现（整文件替换 ContextManager.java）**

> 执行记录：`ContextManager.java` 按计划整文件替换；`SessionManager` 三处（恢复会话 `~222`、新建会话 `~398` 构造 + `applyModelChanged` 的 `cm.update`）按计划改毕。**偏差（计划外补丁）**：计划 Files 未列 `AgentLoop.java`，但 `compress` 改抛受检异常后其 `compactNow` 与自动压缩调用点编译失败，做编译过渡适配——`compactNow` catch `LlmException` → `onError` 提示并保留原上下文（`pushContextStats` 移入 finally 恒执行）；自动压缩 catch 后 `onError`+`break` 中止本轮；移除已删除的 `lastCompressAttempted()` 引用。完整重试编排（重试态/墙钟/中止）留待 Task 7 的 `compressWithRetry` 实现。

```java
package com.minion.core.context;

import com.minion.core.llm.LlmClient;
import com.minion.core.llm.LlmException;
import com.minion.core.llm.Message;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

/** 上下文管理：token 估算、阈值判断、单次压缩（最早链 → 摘要置前）。
 *  阈值 0.65 / 压缩比例 0.8 / 摘要上限 5000 字为硬编码常量（不进模型配置，只保留 maxContextTokens）。
 *  压缩失败不再降级：直接抛 LlmException，由 AgentLoop 按重试策略处理。 */
public class ContextManager {

    /** 摘要输出上限（字符）：写入压缩器 system 提示词 */
    static final int SUMMARY_MAX_CHARS = 5000;
    /** 触发阈值（estimate >= maxContextTokens × THRESHOLD） */
    static final double THRESHOLD = 0.65;
    /** 触发量中要压缩的比例（其余为保留区） */
    static final double COMPRESS_RATIO = 0.8;

    private static final String COMPRESS_SYSTEM =
            "你是 minion 的上下文压缩器。把用户提供的对话历史压缩成一段中文摘要，保留："
          + "未完成的任务与目标、已做出的关键决策及原因、使用过的工具与结果要点、"
          + "相关文件路径、代码约定、用户偏好。只输出摘要正文，不要客套，"
          + SUMMARY_MAX_CHARS + " 字以内。";

    // FX 线程 update/setLlm 写入、会话工作线程 shouldCompress/compress 读取——volatile 防 JMM 数据竞争
    private volatile int maxContextTokens;
    private volatile LlmClient llm;
    private final int systemTokens;    // system 提示词 token 估算在构造时固定，保持不变

    public ContextManager(int maxContextTokens, LlmClient llm, int systemTokens) {
        this.maxContextTokens = maxContextTokens;
        this.llm = llm;
        this.systemTokens = systemTokens;
    }

    /** 模型参数热更新（设置窗修改后调用；运行时生效于下一轮压缩判断） */
    public void update(int maxContextTokens) {
        this.maxContextTokens = maxContextTokens;
    }

    /** 换 LLM 客户端（模型切换后调用；压缩请求走新客户端） */
    public void setLlm(LlmClient llm) { this.llm = llm; }

    /** 上下文窗口上限（AgentLoop 压缩百分比计算用） */
    public int maxTokens() { return maxContextTokens; }

    /** 自动压缩阈值（0~1，恒 0.65；GUI 悬停"剩余x%自动压缩"计算用） */
    public double threshold() { return THRESHOLD; }

    public int estimate(List<Message> messages) {
        return systemTokens + TokenCounter.estimateMessages(messages);
    }

    public boolean shouldCompress(List<Message> messages) {
        return estimate(messages) >= maxContextTokens * THRESHOLD;
    }

    /** 按完整回合链切块。summary 消息跳过（已压缩过，不再参与）；pinned 消息跳过（技能
     *  正文常驻，压缩豁免，由 compress 原样保留）；system 消息跳过（系统提示词不并入链、
     *  不进入压缩批次，由 compress 原样保留）。 */
    public static List<List<Message>> chunkChains(List<Message> messages) {
        List<List<Message>> chains = new ArrayList<List<Message>>();
        List<Message> cur = new ArrayList<Message>();
        for (Message m : messages) {
            if (m.summary || m.pinned || m.role == Message.Role.SYSTEM) {
                flush(chains, cur);
                continue;
            }
            cur.add(m);
            if (m.role == Message.Role.ASSISTANT
                    && (m.toolCalls == null || m.toolCalls.isEmpty())) {
                flush(chains, cur); // 无工具调用的 assistant 结束一条链
            }
        }
        flush(chains, cur);
        return chains;
    }

    private static void flush(List<List<Message>> chains, List<Message> cur) {
        if (!cur.isEmpty()) {
            chains.add(new ArrayList<Message>(cur));
            cur.clear();
        }
    }

    /** 压缩：单次 LLM 调用，无递归、无降级。
     *  - 返回入参同一实例（引用相等）＝ 暂无可压缩（无链）；
     *  - 成功：返回「system 原样 + 新摘要置前 + pinned 原样 + 未压缩链」；
     *  - 失败（请求异常/空摘要）：抛 LlmException，由调用方按重试策略处理。 */
    public List<Message> compress(List<Message> messages) throws LlmException {
        List<List<Message>> chains = chunkChains(messages);
        if (chains.isEmpty()) return messages;
        int take = takeCount(chains);
        String summary = callLlm(existingSummaryText(messages), buildBatch(chains, 0, take));
        List<Message> result = new ArrayList<Message>();
        for (Message m : messages) {
            if (m.role == Message.Role.SYSTEM) result.add(m); // system 原样保留，置于最前
        }
        result.add(summaryMsg(summary));
        for (Message m : messages) {
            if (m.pinned) result.add(m); // 技能加载消息（pinned）常驻：不入链不参与摘要
        }
        for (int i = take; i < chains.size(); i++) {
            result.addAll(chains.get(i)); // 保留未被压缩的链；旧 summary 由新摘要取代
        }
        return result;
    }

    /** 要压缩的链数：从最早链逐链累加 token，累计首次 ≥ compressTokens 时的链数（含该链，
     *  保证压缩量 ≥ COMPRESS_RATIO）；全部链合计仍不足时全压。有链时恒 ≥ 1。 */
    private int takeCount(List<List<Message>> chains) {
        long compressTokens = (long) (maxContextTokens * THRESHOLD * COMPRESS_RATIO);
        long acc = 0;
        int take = 0;
        for (List<Message> chain : chains) {
            acc += TokenCounter.estimateMessages(chain);
            take++;
            if (acc >= compressTokens) return take;
        }
        return take;
    }

    /** 既有摘要文本（二次压缩并入输入，避免旧摘要内容丢失）；无则 null */
    private static String existingSummaryText(List<Message> messages) {
        for (Message m : messages) {
            if (m.summary && m.content != null) return m.content;
        }
        return null;
    }

    /** 单次压缩请求：prefix（旧摘要）非空时置前参与合并；空摘要视为失败抛 EMPTY_RESPONSE */
    private String callLlm(String prefix, String batch) throws LlmException {
        StringBuilder sb = new StringBuilder();
        if (prefix != null) sb.append(prefix).append('\n');
        sb.append(batch);
        String s = llm.completeChat(
                Collections.singletonList(Message.user(sb.toString())), COMPRESS_SYSTEM);
        if (s == null || s.trim().isEmpty()) {
            throw new LlmException(LlmException.Type.EMPTY_RESPONSE, "压缩模型返回空摘要", true);
        }
        return s.trim();
    }

    /** 拼压缩批次文本 */
    private String buildBatch(List<List<Message>> chains, int from, int to) {
        StringBuilder batch = new StringBuilder();
        for (int i = from; i < to; i++) {
            for (Message m : chains.get(i)) {
                batch.append('[').append(m.role).append(']');
                if (m.content != null) batch.append(' ').append(m.content);
                if (m.reasoningContent != null) batch.append(" (思考: ").append(m.reasoningContent).append(')');
                if (m.toolCalls != null && !m.toolCalls.isEmpty()) {
                    batch.append(" [工具: ");
                    for (com.minion.core.llm.ToolCall tc : m.toolCalls) {
                        batch.append(tc.name).append(' ');
                    }
                    batch.append(']');
                }
                batch.append('\n');
            }
        }
        return batch.toString();
    }

    private static Message summaryMsg(String text) {
        Message m = Message.user("【历史对话摘要】\n" + text);
        m.summary = true;
        return m;
    }
}
```

`SessionManager.java` 三处改为：

```java
                ContextManager cm = new ContextManager(mc.maxContextTokens, llm,
                        TokenCounter.estimate(new SystemPromptBuilder(mdAbs, ctx.workspace.workDir(),
                                tmpDirOf(meta.id).toString(), config.emptyOutputPlaceholder(), projSkills)
                                .build(sk.skills)));
```
```java
        ContextManager cm = new ContextManager(mc.maxContextTokens, llm,
                TokenCounter.estimate(new SystemPromptBuilder(mdAbs, ctx.workspace.workDir(),
                        tmpDirOf(s.id).toString(), config.emptyOutputPlaceholder(), projSkills)
                        .build(sk.skills)));
```
```java
                    cm.update(mc.maxContextTokens);
```

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -Dtest=ContextManagerTest,AgentLoopCompactTest`
Expected: PASS

> 执行记录：首轮 1 失败（`compress_keepRegionHeadKeepsToolPairing` 的"应保留部分链"，即 Step 1 记录的链数偏差），修正用例数据后 19 用例全绿（ContextManagerTest 13 + AgentLoopCompactTest 6）。另跑全量 `mvn test` 1044 用例全绿（较 Task 4 基线 -12：旧 ContextManagerTest 25 例被 13 例替换）。

- [x] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/context/ContextManager.java src/main/java/com/minion/gui/session/SessionManager.java src/test/java/com/minion/core/context/ContextManagerTest.java src/test/java/com/minion/core/agent/AgentLoopCompactTest.java
git commit -m "refactor(context): 压缩去递归降级，改为单次调用+抛异常；阈值 0.65/摘要 5000 字硬编码"
```

> 执行记录：提交 `2ca83d5`，实际含 5 个文件（计划 `git add` 漏列过渡适配的 `AgentLoop.java`）。

### Task 6: 压缩配置项移除（模型配置只留 maxContextTokens）

**Files:**
- Modify: `src/main/java/com/minion/core/config/ModelConfig.java:14-15, 30-31`
- Modify: `src/main/java/com/minion/core/config/ModelManager.java:72-73, 88-89, 130-131`
- Modify: `src/main/java/com/minion/gui/dialog/SettingsDialog.java:247-248, 258-259, 273-274, 722-725`
- Modify: `src/resource/config_deepseek.properties:12-13`、`src/resource/config_qwen.properties:15-16`
- Test: `src/test/java/com/minion/core/config/ModelManagerTest.java:43-44`

**Interfaces:**
- Consumes: Task 5 已把 `ContextManager` 改为只吃 `maxContextTokens`
- Produces: `ModelConfig` 不再有 `compressThreshold`/`keepRecentMessages`（旧 model.json 多余字段被 Gson 静默忽略，无需迁移）

- [x] **Step 1: 实现（删除类改动，先删字段再改测试）**

`ModelConfig.java`：删字段与 copy 赋值：

```java
    public int maxContextTokens;
```
```java
        c.maxContextTokens = maxContextTokens;
        return c;
```

`ModelManager.java`：`createDeepseek`/`createQwen` 各删两行（`c.compressThreshold = 0.8;`、`c.keepRecentMessages = 50;`）；`update` 删两行（`old.compressThreshold = ...`、`old.keepRecentMessages = ...`）。

`SettingsDialog.java` 模型表单：删 `thr`/`keep` 两个 TextField 声明、`grid.addRow(8, ...)`、`grid.addRow(9, ...)`、结果转换里的两行赋值，并删除已无调用者的 `parseDouble`：

```java
    private static double parseDouble(String s, double def) {
        try { return Double.parseDouble(s.trim()); } catch (Exception e) { return def; }
    }
```

`src/resource/config_deepseek.properties` 删 12-13 行、`config_qwen.properties` 删 15-16 行：

```
context.compressThreshold=0.8
context.keepRecentMessages=50
```

- [x] **Step 2: 运行测试确认失败（编译失败）**

Run: `mvn -q test -Dtest=ModelManagerTest`
Expected: 编译失败——`ModelManagerTest` 仍引用 `q.compressThreshold` / `q.keepRecentMessages`

> 执行记录：实际为**运行期**失败而非编译失败——Maven 增量编译未重编未变更的 `ModelManagerTest.java`，其旧字节码在运行时抛 `NoSuchFieldError: compressThreshold`（ModelManagerTest.java:43），与"测试未同步"的意图一致。

- [x] **Step 3: 改测试**

`ModelManagerTest.load_createsDefaultModels` 删除：

```java
        assertEquals(0.8, q.compressThreshold, 1e-9);
        assertEquals(50, q.keepRecentMessages);
```

（第 126-129 行的 `load_validExistingFileUntouched` 保留 JSON 中旧字段不动——它正好回归"旧字段被忽略"。）

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -Dtest=ModelManagerTest,ContextManagerTest,AgentLoopCompactTest`
Expected: PASS

> 执行记录：三类测试全绿（-q 静默无输出）；另跑全量 `mvn test` 1044 用例全绿（与 Task 5 基线一致，未增删用例）。

- [x] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/config/ModelConfig.java src/main/java/com/minion/core/config/ModelManager.java src/main/java/com/minion/gui/dialog/SettingsDialog.java src/resource/config_deepseek.properties src/resource/config_qwen.properties src/test/java/com/minion/core/config/ModelManagerTest.java
git commit -m "refactor(config): 模型配置只留 maxContextTokens，移除 compressThreshold/keepRecentMessages"
```

> 执行记录：提交 `9cb5104`，6 文件、30 行删除（`parseDouble` 一并删除）。残留说明：`ModelManagerTest:126` JSON 里的旧字段按计划保留（回归"旧字段被忽略"）；`src/test/resources/config-test.properties` 的老两行为计划 Files 未列且无代码读取，本次未动（Task 10 残留检查用 `--include=*.java --include=*.md`，不会命中）。

---

### Task 7: 压缩通道纳入重试（失败中止本轮，不再降级）

**Files:**
- Modify: `src/main/java/com/minion/core/agent/AgentLoop.java`（`compactNow` ~279-301、自动压缩调用点 ~398-412、新增 `compressWithRetry`）
- Test: `src/test/java/com/minion/core/agent/AgentLoopCompactTest.java`（新增 3 用例）

**Interfaces:**
- Consumes: `RetryPolicy.isTransient/kindOf/delayMs`（Task 2）、`RetryProgress.from(int, LlmException, long)`（Task 3）、`ContextManager.compress` 抛 `LlmException`（Task 5）
- Produces: 私有 `enum CompressOutcome { OK, NOTHING, FAILED }` 与 `private CompressOutcome compressWithRetry() throws InterruptedException`

- [x] **Step 1: 写失败测试**

`AgentLoopCompactTest` 末尾（最后一个 `}` 之前）新增：

```java
    /** 压缩瞬时失败（空摘要 → EMPTY_RESPONSE）→ 同策略小参数重试后成功 */
    @Test
    public void autoCompress_transientFailureThenSuccess() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】重试成功";
        llm.failAtCompleteChat.add(1); // 第 1 次压缩返回空串 → EMPTY_RESPONSE（可重试）
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        RecordingUi ui = new RecordingUi();
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        ContextManager cm = new ContextManager(50, llm, 0); // 50×0.65=32.5 触发
        AgentLoop loop = new AgentLoop(llm, registry,
                new SystemPromptBuilder(tmp.getRoot().getPath() + "/project.md"),
                confirm, ui, cm,
                new Workspace(tmp.getRoot().getPath()),
                Session.create(tmp.getRoot().getPath(), "test-model"));
        loop.retryPolicy = new RetryPolicy(10, 10, 60000);
        loop.roundLimit = 10;
        for (int i = 0; i < 3; i++) {
            llm.addTurn("回复" + i);
            loop.runUserTurn("问题" + i);
        }
        assertEquals(2, llm.completeChatRequests.size()); // 失败 1 次 + 重试 1 次
        assertTrue(loop.messages().get(0).summary);
        assertFalse(ui.retryProgress.isEmpty());                     // 进过重试态
        assertEquals(Integer.valueOf(0), ui.retryAttempts().get(ui.retryAttempts().size() - 1));
        assertTrue(ui.errors.isEmpty());
    }

    /** 压缩持续失败至墙钟耗尽：报错并中止本轮（不发送请求），指示器复位 */
    @Test
    public void autoCompress_retryExhausted_stopsTurnWithoutRequest() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = ""; // 恒空摘要 → 恒 EMPTY_RESPONSE
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        RecordingUi ui = new RecordingUi();
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        ContextManager cm = new ContextManager(50, llm, 0);
        AgentLoop loop = new AgentLoop(llm, registry,
                new SystemPromptBuilder(tmp.getRoot().getPath() + "/project.md"),
                confirm, ui, cm,
                new Workspace(tmp.getRoot().getPath()),
                Session.create(tmp.getRoot().getPath(), "test-model"));
        loop.retryPolicy = new RetryPolicy(10, 10, 50); // 快速耗尽
        loop.roundLimit = 10;
        for (int i = 0; i < 3; i++) {
            llm.addTurn("回复" + i);
            loop.runUserTurn("问题" + i);
        }
        assertEquals(2, llm.requests.size()); // 前两轮各 1 次请求；第 3 轮压缩失败后中止，未发送
        assertEquals(1, ui.errors.size());
        assertTrue(ui.errors.get(0).contains("自动压缩失败"));
        assertTrue(ui.errors.get(0).contains("仍失败"));
        assertEquals(Integer.valueOf(0), ui.retryAttempts().get(ui.retryAttempts().size() - 1));
    }

    /** 压缩不可重试错误（OTHER/retryable=false）：立即失败中止本轮，不进重试态 */
    @Test
    public void autoCompress_nonRetryableError_stopsImmediately() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.throwOnCompleteChat = true; // LlmException(OTHER, retryable=false)
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        RecordingUi ui = new RecordingUi();
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        ContextManager cm = new ContextManager(50, llm, 0);
        AgentLoop loop = new AgentLoop(llm, registry,
                new SystemPromptBuilder(tmp.getRoot().getPath() + "/project.md"),
                confirm, ui, cm,
                new Workspace(tmp.getRoot().getPath()),
                Session.create(tmp.getRoot().getPath(), "test-model"));
        loop.retryPolicy = new RetryPolicy(10, 10, 60000);
        loop.roundLimit = 10;
        for (int i = 0; i < 3; i++) {
            llm.addTurn("回复" + i);
            loop.runUserTurn("问题" + i);
        }
        assertTrue(ui.retryProgress.isEmpty());                 // 未进重试态
        assertTrue(ui.errors.get(0).contains("自动压缩失败"));
        assertEquals(2, llm.requests.size());                   // 第 3 轮中止：未发送请求
    }
```

（这些用例需要 `import com.minion.core.agent.RetryPolicy;`——同包，无需 import；`FakeLlmClient.failAtCompleteChat` 字段已存在。）

> 执行记录：3 用例按计划落地；**偏差**：轮数 3 → 4——阈值 32.5（50×0.65）下第 3 轮循环检查时仅 30 token 不触发（4 user+3 assistant=42 才 ≥ 阈值，与既有用例注释一致）。`transientFailureThenSuccess` 补 `llm.addTurn("压缩后回复")`（压缩成功后仍发本轮请求）；`retryExhausted`/`nonRetryable` 断言请求数 2→3（前三轮各 1 次 + 第 4 轮中止未发送），压缩轮改用 `loop.runUserTurn("触发压缩")`。

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -Dtest=AgentLoopCompactTest`
Expected: FAIL（压缩失败仍按旧路径静默返回：无重试、无 onError、第 3 轮继续发送）

> 执行记录：首跑 3 轮版本失败于"未触发压缩"（completeChat=0、requests=3、errors 空），据此校正轮数；校正后与预期一致——`transientFailureThenSuccess` 失败于 `completeChatRequests` 1≠2（无重试），`retryExhausted` 失败于耗尽文案缺"仍失败"（过渡适配旧文案），`nonRetryable` 用例在 Task 5 过渡适配下已提前通过（过渡路径对不可重试错误即 onError+break，与目标行为一致，实现后仍须通过）。

- [x] **Step 3: 实现**

`AgentLoop.java` 新增（放在 `compactNow` 之前）：

```java
    /** 压缩结果：OK=已压缩；NOTHING=暂无可压缩；FAILED=失败/中断（已 onError 提示或用户中断） */
    private enum CompressOutcome { OK, NOTHING, FAILED }

    /** 压缩 + 瞬时错误重试（自动压缩与 /compact 共用）：与请求重试同一策略（分类间隔/墙钟），
     *  可被"停止"中断；耗尽或不可重试错误 → onError 并返回 FAILED（调用方中止本轮不发送请求）。
     *  成功不打提示文案（自动压缩与手动压缩文案不同，由调用方各自输出）。 */
    private CompressOutcome compressWithRetry() throws InterruptedException {
        List<Message> before = session.messages;
        int attempts = 0;
        long retryStart = System.currentTimeMillis();
        boolean inRetry = false;
        try {
            while (true) {
                try {
                    session.messages = contextManager.compress(session.messages);
                    return session.messages == before ? CompressOutcome.NOTHING : CompressOutcome.OK;
                } catch (LlmException e) {
                    if (!RetryPolicy.isTransient(e)) {
                        ui.onError("自动压缩失败：" + e.getMessage()
                                + "；本轮已停止，可稍后重试或新建会话");
                        return CompressOutcome.FAILED;
                    }
                    attempts++;
                    long delay = retryPolicy.delayMs(RetryPolicy.kindOf(e));
                    inRetry = true;
                    ui.onRetryProgress(RetryProgress.from(attempts, e, delay));
                    if (!sleepWithInterruptCheck(delay)) break; // 用户中断
                    long elapsed = System.currentTimeMillis() - retryStart;
                    if (retryPolicy.isExhausted(elapsed)) {
                        ui.onError("自动压缩失败：" + RetryProgress.tag(e) + " 重试了 " + attempts
                                + " 次，持续 " + (elapsed / 60000) + " 分钟仍失败；本轮已停止");
                        return CompressOutcome.FAILED;
                    }
                }
            }
        } finally {
            if (inRetry) ui.onRetryProgress(RetryProgress.none()); // 退出重试态复位（幂等）
        }
        return CompressOutcome.FAILED; // 用户中断
    }
```

`compactNow` 整体替换为：

```java
    public void compactNow() {
        if (contextManager == null) {
            ui.onWarning("未启用上下文压缩");
            return;
        }
        ui.onCompressingChanged(true);
        CompressOutcome outcome;
        try {
            outcome = compressWithRetry();
        } catch (InterruptedException e) {
            Thread.currentThread().interrupt();
            outcome = CompressOutcome.FAILED;
        } finally {
            ui.onCompressingChanged(false);
        }
        if (outcome == CompressOutcome.OK) {
            ui.onWarning("已压缩上下文（历史摘要已置前）");
        } else if (outcome == CompressOutcome.NOTHING) {
            ui.onWarning("暂无可压缩内容");
        }
        // FAILED：compressWithRetry 内已 onError 提示
        pushContextStats();
    }
```

自动压缩调用点（`while (!interrupted)` 内）替换为：

```java
                if (contextManager != null && contextManager.shouldCompress(session.messages)) {
                    ui.onCompressingChanged(true);
                    CompressOutcome outcome;
                    try {
                        outcome = compressWithRetry();
                    } catch (InterruptedException ex) {
                        Thread.currentThread().interrupt();
                        outcome = CompressOutcome.FAILED;
                    } finally {
                        ui.onCompressingChanged(false);
                    }
                    if (outcome == CompressOutcome.OK) {
                        int pct = (int) (contextManager.estimate(session.messages) * 100
                                / contextManager.maxTokens());
                        ui.onWarning("自动压缩已完成，上下文降低至" + pct + "%");
                        pushContextStats(); // 压缩完成：进度圈回落
                    } else if (outcome == CompressOutcome.FAILED) {
                        break; // 压缩失败/用户中断：中止本轮，不发送请求（失败已 onError 提示）
                    }
                    // NOTHING（shouldCompress 为真时理论不可达）：继续本轮请求
                }
```

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -Dtest=AgentLoopCompactTest`
Expected: PASS（含既有 6 用例：自动/手动压缩、指示器事件、上下文统计）

> 执行记录：实现按计划片段落地（`CompressOutcome` 枚举 + `compressWithRetry`、`compactNow` 与自动压缩调用点替换），无额外偏差。`AgentLoopCompactTest` 9 用例全绿（新增 3 + 既有 6）；全量 `mvn test` 1047 用例 0 失败（较 Task 6 基线 1044 +3，恰为新增用例），`mvn compile` 随构建通过。

- [x] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/agent/AgentLoop.java src/test/java/com/minion/core/agent/AgentLoopCompactTest.java
git commit -m "feat(agent): 压缩通道纳入重试编排，失败中止本轮不再降级"
```

> 执行记录：提交 `1a15488`，2 文件（+157/-30），与计划 `git add` 清单一致。

---

### Task 8: 去掉 Esc 终止运行

**Files:**
- Modify: `src/main/java/com/minion/gui/input/InputView.java`（Esc 分支 ~182-185、tooltip ~665、类注释 ~42）

**Interfaces:**
- Consumes: 无
- Produces: 无（Esc 仅保留关闭补全弹层）

- [x] **Step 1: 删代码**

`InputView.java` 第 181-185 行（含注释行）整体删除：

```java
            // Esc：终止当前运行（提问挂起时亦可终止）
            if (e.getCode() == KeyCode.ESCAPE && current != null && running) {
                e.consume();
                manager.stop(current);
            }
```

（补全弹层的 Esc 分支保留不动。）

第 665 行 tooltip 文案：

```java
            case STOP:       applyStyle(stopIcon, buttonStyleClass(mode), 1.0, "终止当前运行"); break;
```

（原为 `"终止当前运行 (Esc)"`，去掉 Esc 提示。）

第 42 行类注释：

```java
 *  按钮语义：上箭头=发送/补充/回答、变淡箭头=空输入或等待回答、方块=终止（Esc 不再终止运行，用方块按钮）；
```

- [x] **Step 2: 编译验证**

Run: `mvn -q compile`
Expected: 成功、无输出

> 执行记录：`mvn -q compile` 成功（无输出）；相关用例 `InputViewKeyTest`/`InputViewButtonTest`/`InputViewInlineTest` 29 用例全绿。偏差：`InputViewButtonTest.asking_empty_answerDim` 注释"终止入口为 Esc"同步改为"终止入口为发送行停止按钮"（计划 Files 未列，属文案同步，1 行）。

- [ ] **Step 3: 手工验收（无自动化覆盖，FX 事件监听）**

1. `mvn package` 后运行 `target/minion-0.1.0.jar`；
2. 发起一轮长回复，按 Esc：**不再终止**（运行继续）；
3. 输入 `/` 弹出补全层后按 Esc：弹层关闭；
4. 点击发送行"停止"按钮：仍可终止当前运行。

- [ ] **Step 4: 提交**

```bash
git add src/main/java/com/minion/gui/input/InputView.java
git commit -m "refactor(gui): 去掉 Esc 终止运行快捷键，保留补全弹层 Esc 关闭"
```

> 执行记录：提交 `467e8f3`，2 文件（+3/-8，含上述按钮测试注释同步）。Step 3 手工验收（FX 事件监听，无自动化覆盖）本机未执行，待用户运行 jar 验收：运行中按 Esc 不终止、`/` 补全层 Esc 可关、停止按钮仍可终止。

---

### Task 9: 新建模型默认 200k + 内置模型名简化

**Files:**
- Modify: `src/main/java/com/minion/core/config/ModelManager.java:64-67, 80-83`
- Modify: `src/main/java/com/minion/gui/dialog/SettingsDialog.java:246, 272`
- Modify: `README.md:164`（示例注释）
- Test: `src/test/java/com/minion/core/config/ModelManagerTest.java`（多处）

**Interfaces:**
- Consumes: 无
- Produces: 内置默认 `displayName/modelName = "deepseek"`、`"qwen"`，`maxContextTokens = 200000`；新建模型表单默认 `200000`

- [x] **Step 1: 改测试（先失败）**

`ModelManagerTest` 改动点：

```java
        assertEquals("deepseek", c.displayName);
        assertEquals(200000, c.maxContextTokens);
        ModelConfig q = m.get("qwen");
        assertEquals("qwen", q.modelName);
        assertEquals(200000, q.maxContextTokens);
```

其余名字引用同步替换：
- `m.remove("deepseek-v4-flash")`（71 行）→ `m.remove("deepseek")`
- `m.setCurrent("qwen3-max")`（82 行）→ `m.setCurrent("qwen")`
- `m.remove("deepseek-v4-flash")`（83 行）→ `m.remove("deepseek")`
- `assertEquals("qwen3-max", m.current().displayName)`（85 行）→ `assertEquals("qwen", ...)`
- `assertEquals("deepseek-v4-flash", m.current().displayName)`（106、119 行）→ `assertEquals("deepseek", ...)`
- JSON 里 `"currentModelName":"deepseek-v4-flash"`（114 行）→ `"currentModelName":"deepseek"`
- 147-148 行（旧配置重载用例）保持原字面量不动（与内置默认值无关）

> 执行记录：清单逐项落地；计划引用行号整体偏移 +2（如 remove 实际 69 行、setCurrent 实际 80 行、`currentModelName` 实际 112 行、旧配置用例实际 145-146 行），按内容匹配修改，无实质偏差。

- [x] **Step 2: 运行测试确认失败**

Run: `mvn -q test -Dtest=ModelManagerTest`
Expected: FAIL（默认名仍为 `deepseek-v4-flash`/`qwen3-max`、上下文仍 900000/131072）

> 执行记录：运行失败 5 例，与预期一致：3 例名字不符（load_createsDefaultModels、load_structureInvalidJsonBacksUpAndRebuilds、load_nullDisplayNameEntryFiltered，expected `deepseek` but was `deepseek-v4-flash`），2 例 remove 失败（remove_lastModelRejected、remove_otherModelOkAndCurrentFallsBack）。

- [x] **Step 3: 实现**

`ModelManager.createDeepseek()`：

```java
        c.displayName = "deepseek";
        c.url = "https://api.deepseek.com/v1/chat/completions";
        c.apiKey = "";
        c.modelName = "deepseek";
        c.provider = "deepseek";
        c.thinking = true;
        c.reasoningEffort = "max";
        c.maxContextTokens = 200000;
```

`ModelManager.createQwen()`：

```java
        c.displayName = "qwen";
        c.url = "https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions";
        c.apiKey = "";
        c.modelName = "qwen";
        c.provider = "qwen";
        c.thinking = true;
        c.reasoningEffort = "xhigh";
        c.maxContextTokens = 200000;
```

`SettingsDialog` 模型表单：`TextField maxCtx = new TextField(mc == null ? "200000" : String.valueOf(mc.maxContextTokens));`；`out.maxContextTokens = parseInt(maxCtx.getText(), 200000);`

`README.md` 第 164 行注释改为：

```
    # 按你的模型窗口填写（千问常见 128k~256k）
```

风险注记（已知并接受）：`modelName` 非官方模型 ID，首次使用若服务端报"模型不存在"需在设置窗改成真实 ID；qwen 实际窗口可能仅 131k，200000 需手改。

> 执行记录：按片段落地（`createQwen` 原 131072 一并改 200000；SettingsDialog 表单默认值与 `parseInt` 兜底均 900000→200000）；README 注释实际为第 163 行（计划标 164，偏移 -1），替换后示例值 `maxContextTokens=131072` 保留。`src/resource/config_*.properties` 模板中的旧名字/900000 属历史模板（当前无代码读取），不在本次 Files 内，未动。

- [x] **Step 4: 运行测试确认通过**

Run: `mvn -q test -Dtest=ModelManagerTest,SettingsDialogTest`
Expected: PASS

> 执行记录：通过（ModelManagerTest 10 用例 + SettingsDialogTest 5 用例全绿）；另补跑依赖 `ModelManager.load` 的 `SessionManagerTest` 43 用例全绿，确认默认名变更无连带影响。

- [x] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/config/ModelManager.java src/main/java/com/minion/gui/dialog/SettingsDialog.java README.md src/test/java/com/minion/core/config/ModelManagerTest.java
git commit -m "feat(config): 新建模型默认上下文 200k，内置模型名简化为 deepseek/qwen"
```

> 执行记录：提交 `35bc739`，4 文件（+21/-21），与计划 `git add` 清单一致。

---

### Task 10: 文档同步与全量验证

**Files:**
- Modify: `docs/ARCHITECTURE.md:14, 42, 110`
- Modify: `README.md:49, 71`

**Interfaces:**
- Consumes: Task 1-9 的全部行为变更
- Produces: 文档与实现一致（历史 spec/plan 不回改）

- [ ] **Step 1: 更新 ARCHITECTURE.md**

第 14 行 `RetryPolicy（长重试策略）` → `RetryPolicy（重试策略：按类别 5s/30s、墙钟 12 分钟）`。

第 42 行 InputView 描述中 `Esc 关闭补全弹层/终止运行` → `Esc 关闭补全弹层`。

另：`src/main/java/com/minion/core/tools/ToolResult.java:17` 注释中「直至 20 分钟超时」→「直至 12 分钟超时」（重试墙钟已改 12 分钟；本任务随文档一并同步）。

第 110 行整条替换为：

```markdown
- `ContextManager` / `TokenCounter`：上下文压缩（达 maxContextTokens×0.65 触发；从最早完整回合链按 token 累加到 0.65×0.8 后整体压缩、摘要置前、摘要上限 5000 字；单次调用不递归，失败抛 LlmException 由 AgentLoop 按重试策略处理，耗尽中止本轮）
```

- [ ] **Step 2: 更新 README.md**

第 49 行 `Esc 关闭补全弹层 / 终止当前运行` → `Esc 关闭补全弹层（停止运行请点发送行的停止按钮）`。

第 71 行指示器描述末尾追加：`；瞬时错误重试中显示「重试第 N 次 + 约 N 秒后重试」（429/超时 5 秒，500 类/空响应 30 秒，墙钟 12 分钟）`。

- [ ] **Step 3: 全量回归**

Run: `mvn -q test`
Expected: 全部用例 PASS（含 RetryPolicy/AgentLoop/SubAgentLoop/ContextManager/AgentLoopCompact/ModelManager/RunningIndicator/DeepSeekClient/SettingsDialog/InputView 各测试类）

- [ ] **Step 4: 残留检查**

Run:

```bash
grep -rn "20 分钟\|固定 5s\|3000 字\|compressThreshold\|keepRecentMessages\|lastCompressAttempted\|splitOversizedChains\|isTransientError" --include=*.java --include=*.md src/ docs/ README.md
```
Expected: `src/` 与 `docs/ARCHITECTURE.md`、`README.md` 无命中（Task 4 已改掉 `AgentLoopTest:560`、Task 10 已改掉 `ToolResult:17` 的"20 分钟"文案）；命中仅允许出现在 `docs/superpowers/specs/`（历史/本次设计文档，不回改）与 `docs/superpowers/plans/`（本计划文本自述，属预期）

- [ ] **Step 5: 手工验收清单**

1. 启动 jar，正常对话一轮 → 无回归；
2. 拔网线/改错 API 地址触发网络错误 → 指示器显示"重试第 1 次，约 5 秒后重试"；
3. 模拟 500（改错 URL 指向返回 500 的地址）→ 显示"约 30 秒后重试"；
4. 长对话触发自动压缩 → 显示"上下文压缩中..."，完成后显示"自动压缩已完成，上下文降低至 N%"；
5. `/compact` → 同机制、可中断（压缩等待期间点"停止"能终止）；
6. 设置窗新建模型 → maxContextTokens 默认 200000，且模型表单不再有 compressThreshold/keepRecentMessages 两行；
7. 删除 model.json 重启 → 生成 deepseek/qwen 两套配置，均 200000。

- [ ] **Step 6: 提交**

```bash
git add docs/ARCHITECTURE.md README.md
git commit -m "docs: 同步压缩/重试行为与 Esc 说明"
```

---

## 验收标准（全部满足才算完成）

1. `mvn compile` + `mvn test` 全绿；
2. 重试：429/超时/可恢复网络 5s、500 类（含 503/504）与空响应 30s、等待时长随最近一次失败类别变化、墙钟 12 分钟耗尽一次性总结；
3. 空响应（HTTP 2xx 无 body）进长重试；零增量闸门与中断行为不变；
4. 压缩：阈值恒 0.65、压缩量 ≈0.65×0.8、摘要上限 5000 字、无递归降级、二次压缩不丢旧摘要、压缩失败可重试且耗尽/不可重试时中止本轮（不发送请求）；
5. 模型配置仅剩 maxContextTokens；旧 model.json 多余字段被忽略、无迁移；
6. Esc 不再终止运行，补全弹层 Esc 仍生效，停止按钮可用；
7. 新建模型默认 200000、内置 deepseek/qwen；
8. 文档（README/ARCHITECTURE）与代码行为一致。

