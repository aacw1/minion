# 压缩保留区 token 预算化（v2 响应式收益门槛）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把压缩保留区从「固定最近 6 组」改为「0.13×max token 预算 + 保底最新 1 组」，并把 v1 的回合级防抖改造为 v2 响应式收益门槛（可压量 ≥5%×max 才压、≥85%×max 强制压、提示每回合/全程一次），使长任务回合可多次压缩且零收益压缩不再空转。

**Architecture:** 算法与判定全部收在 `core/context/ContextManager`（分组结果复用：`keepCount` → `take` → `compressibleTokens`/`worthCompressing`）；`AgentLoop`/`SubAgentLoop` 只做「超阈值 → 值得压？→ 压缩/暂缓提示」的接线，不复制任何阈值计算。

**Tech Stack:** JDK 8 + Maven 单模块；JUnit 4；`FakeLlmClient`/`RecordingUi` 测试桩。

**规格：** `docs/superpowers/specs/2026-09-14-压缩保留区预算化-design.md`（v2，已获批）

## Global Constraints

- JDK 8 语法（不用 `var`/`List.of`/文本块）；测试用 JUnit 4（`org.junit.Test` + `org.junit.Assert`）。
- 注释/文档/提交信息用中文；提交用 conventional 格式（如 `feat(context): …`）。
- 新常量硬编码在 `ContextManager`（不进 `config.properties`）。
- 测试命令统一在项目根执行：`mvn -q test -Dtest=<类名> -DfailIfNoTests=false`（`-q` 下**无输出 = 通过**；stderr 里出现 `[minion] 输出落盘失败` 可能是既有用例的预期降级日志，不等于失败，以退出码为准）。
- 源码目录是 `src/main/java` / `src/test/java`（资源目录 `src/resource`，本计划不涉及）。
- `TokenCounter` 口径（断言用）：每条消息 `+4` overhead；中文 1 字 ≈ 0.7、其他 1 字符 ≈ 0.25，向上取整。例：`Message.user("问题0")` = ceil(1.65) + 4 = **6**；`Message.user("x")` = 1 + 4 = **5**；`Message.user("")` = **4**；`Message.assistant("一二三四五六七八九十")` = 7 + 4 = **11**。

---

### Task 1: ContextManager 保留区预算化 + 收益判定

**Files:**
- Modify: `src/main/java/com/minion/core/context/ContextManager.java`（常量区、`compress`、新增 `keepCount`/`compressibleTokens`/`worthCompressing`，删除 `takeCount`）
- Test: `src/test/java/com/minion/core/context/ContextManagerTest.java`

**Interfaces:**
- Produces（Task 2/3 依赖，签名逐字一致）：
  - `public long compressibleTokens(List<Message> messages)` — 本次压缩能压掉的 token 量
  - `public boolean worthCompressing(List<Message> messages)` — 危险区（≥`FORCE_COMPRESS_RATIO`）或可压量 ≥ `MIN_COMPRESSIBLE_RATIO` 才为 true（**调用方须先确认 `shouldCompress` 为 true**）
  - 包内常量：`KEEP_BUDGET_RATIO=0.2`、`KEEP_MIN_GROUPS=1`、`MIN_COMPRESSIBLE_RATIO=0.05`、`FORCE_COMPRESS_RATIO=0.85`；删除 `KEEP_RECENT_GROUPS`、`COMPRESS_RATIO`

- [ ] **Step 1: 重写/新增 ContextManagerTest 用例（先红）**

改动 6 个既有用例：
- **替换**（其语义锁定在「硬留 6 组」）4 个：`hardcoded_metrics`、`compress_takesEarliestGroupsUntilCompressBudget`、`compress_keepsRecent6GroupsWhenBudgetNotReached`、`compress_tooFewGroups_returnsSameInstanceWithoutLlmCall` —— 改为下面列出的 9 个用例；
- **调整** 1 个：`compress_boundaryFallsOnGroupBoundary_keepsToolPairing`（`max` 200→400，并把「组 8..23 精确保留」的断言换成 `lastN` 连续后缀 + 工具组配对断言，见第 10 段）；
- 其余用例保持不动。

```java
    @Test
    public void hardcoded_metrics() {
        ContextManager cm = new ContextManager(100, new FakeLlmClient(), 0);
        assertEquals(0.65, cm.threshold(), 1e-9);
        assertEquals(5000, ContextManager.SUMMARY_MAX_CHARS);
        assertEquals("保留区预算比例（0.13×max）", 0.2, ContextManager.KEEP_BUDGET_RATIO, 1e-9);
        assertEquals("保底保留最新 1 组", 1, ContextManager.KEEP_MIN_GROUPS);
        assertEquals("事前收益门槛", 0.05, ContextManager.MIN_COMPRESSIBLE_RATIO, 1e-9);
        assertEquals("危险区豁免", 0.85, ContextManager.FORCE_COMPRESS_RATIO, 1e-9);
        assertTrue(cm.shouldCompress(chains(5, "问题")));   // 5 链（90 token）> 100×0.65
        assertFalse(cm.shouldCompress(chains(2, "问题")));  // 2 链（36 token）< 65
    }

    /** 保留区 = 预算内的最近 K 组：max=100（阈值 65、预算 13），chains(12) 24 组；
     *  最新组（终答 11 token）≤ 13，再往前加一组（6）就超预算 → keep=1 */
    @Test
    public void compress_keepsRecentGroupsWithinBudget() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(100, llm, 0);
        List<Message> input = chains(12, "问题"); // 24 组 216 token
        List<Message> result = cm.compress(input);
        assertTrue(result.get(0).summary);
        List<Message> kept = result.subList(1, result.size());
        assertTrue("保留区不得超预算 13", TokenCounter.estimateMessages(kept) <= 13);
        assertSameSequence(lastN(input, kept.size()), kept); // 原消息连续后缀（组边界切割）
        assertTrue("早期组已压缩", llm.completeChatRequests.get(0).contains("[USER] 问题0"));
    }

    /** 预算能装下最近多组时尽量多留（K>1）：max=1000（阈值 650、预算 130），chains(40) 80 组 680 token */
    @Test
    public void compress_keepsMultipleGroupsWithinBudget() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(1000, llm, 0);
        List<Message> input = chains(40, "问题");
        assertTrue(cm.shouldCompress(input));
        List<Message> result = cm.compress(input);
        List<Message> kept = result.subList(1, result.size());
        assertTrue("保留区不得超预算 130", TokenCounter.estimateMessages(kept) <= 130);
        assertTrue("应保留多组（不是保底 1 组）: " + kept.size(), kept.size() >= 2);
        assertSameSequence(lastN(input, kept.size()), kept);
    }

    /** 全部组都在保留预算内 → 无可压缩：返回同一引用、不调 LLM */
    @Test
    public void compress_allGroupsWithinBudget_returnsSameInstance() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        ContextManager cm = new ContextManager(10000, llm, 0); // 预算 1300 >> 全部组 216 token
        List<Message> input = chains(12, "问题");
        assertSame(input, cm.compress(input));
        assertTrue(llm.completeChatRequests.isEmpty());
    }

    /** 最新组自身超保留预算 → 保底保留它（take = 组数 − 1） */
    @Test
    public void compress_keepsNewestGroupEvenIfOverBudget() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(60, llm, 0); // 预算 7.8：最新组 11 token 已超
        List<Message> input = chains(3, "问题"); // 6 组 51 token ≥ 阈值 39
        assertTrue(cm.shouldCompress(input));
        List<Message> result = cm.compress(input);
        assertEquals("摘要 + 最新 1 组", 2, result.size());
        assertSame(input.get(input.size() - 1), result.get(1));
        assertEquals(1, llm.completeChatRequests.size());
    }

    /** 可压量 = take 组合计 = 压缩前总量 − 保留区总量 */
    @Test
    public void compressibleTokens_matchesCompressedVolume() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(100, llm, 0); // 预算 13
        List<Message> input = chains(12, "问题");
        int total = TokenCounter.estimateMessages(input);
        long compressible = cm.compressibleTokens(input);
        assertTrue("可压量 > 0", compressible > 0);
        List<Message> result = cm.compress(input);
        int kept = TokenCounter.estimateMessages(result.subList(1, result.size()));
        assertEquals("可压量 = 压缩前总量 − 保留区总量", total - kept, compressible);
    }

    /** 全部组在保留预算内 → 可压量 0 */
    @Test
    public void compressibleTokens_zeroWhenAllGroupsWithinBudget() {
        ContextManager cm = new ContextManager(10000, new FakeLlmClient(), 0);
        assertEquals(0L, cm.compressibleTokens(chains(12, "问题")));
    }

    /** worthCompressing：可压量达门槛（5%×max）→ true */
    @Test
    public void worthCompressing_trueWhenYieldReachesThreshold() {
        ContextManager cm = new ContextManager(100, new FakeLlmClient(), 0); // 门槛 5
        long yield = cm.compressibleTokens(chains(12, "问题"));
        assertTrue("可压量应远超门槛 5: " + yield, yield > 5);
        assertTrue(cm.worthCompressing(chains(12, "问题")));
    }

    /** worthCompressing：可压量不足门槛且未进危险区 → false；进入危险区（≥85%×max）→ true */
    @Test
    public void worthCompressing_dangerZoneForcesEvenIfYieldIsLow() {
        // pinned 3600 字符 = 900 token；30 组 = 9×5 + 21×6 = 171 token；预算 156 → 可压量仅 15
        List<Message> input = new ArrayList<Message>();
        input.add(pinned(3600));
        for (int i = 0; i < 30; i++) input.add(Message.user(i < 9 ? "x" : "问" + (i % 10)));
        ContextManager low = new ContextManager(1200, new FakeLlmClient(), 0); // 阈值 780、门槛 60、危险区 1020
        assertTrue("场景前提：超阈值", low.shouldCompress(input));
        assertEquals("场景前提：可压量仅 15", 15L, low.compressibleTokens(input));
        assertFalse("危险区外不压", low.worthCompressing(input));
        ContextManager high = new ContextManager(1000, new FakeLlmClient(), 0); // 危险区 850
        assertTrue("危险区（≥85%）强制压", high.worthCompressing(input));
    }

    /** 测试辅助：pinned 大消息（ASCII 字符 n 个 ≈ n×0.25 token） */
    private static Message pinned(int chars) {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < chars; i++) sb.append('x');
        Message m = Message.user(sb.toString());
        m.pinned = true;
        return m;
    }

    /** 切割边界只落在原子组边界：max=400（阈值 260、保留预算 52、门槛 20），8 链（每链 3 组：
     *  user 11 / 工具组 17 / 终答 11）共 24 组 312 token → keep=4（组 20..23）、take=20 */
    @Test
    public void compress_boundaryFallsOnGroupBoundary_keepsToolPairing() throws Exception {
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ContextManager cm = new ContextManager(400, llm, 0);
        List<Message> msgs = new ArrayList<Message>();
        for (int i = 0; i < 8; i++) {
            msgs.add(Message.user("一二三四五六七八九十"));                        // 11 token
            msgs.add(assistantWithTools("Read"));                                // 6 token
            msgs.add(Message.toolResult("c_Read", "Read", "一二三四五六七八九十")); // 11 token
            msgs.add(Message.assistant("一二三四五六七八九十"));                   // 11 token
        }
        assertEquals("每链 3 组（user / 工具组 / 终答），共 24 组",
                24, ContextManager.chunkGroups(msgs).size());
        assertTrue(cm.shouldCompress(msgs));
        List<Message> result = cm.compress(msgs);
        assertTrue(result.get(0).summary);
        List<Message> kept = result.subList(1, result.size());
        assertNotEquals("保留区首条必须是组首，不能是孤立 TOOL", Message.Role.TOOL, kept.get(0).role);
        assertSameSequence(lastN(msgs, kept.size()), kept); // 保留区 = 原消息连续后缀（组边界切割）
        for (int i = 0; i < kept.size(); i++) {
            Message m = kept.get(i);
            if (m.role == Message.Role.ASSISTANT && m.toolCalls != null && !m.toolCalls.isEmpty()) {
                for (int j = 0; j < m.toolCalls.size(); j++) {
                    assertEquals("工具组的 tool 结果必须紧随 assistant", Message.Role.TOOL,
                            kept.get(i + 1 + j).role);
                }
            }
        }
    }
```

- [ ] **Step 2: 跑到红**

Run: `mvn -q test -Dtest=ContextManagerTest -DfailIfNoTests=false`
Expected: 编译失败（`KEEP_BUDGET_RATIO` 等常量不存在 / `compressibleTokens` 未定义）——编译期红即为本步目标。

- [ ] **Step 3: 实现 ContextManager**

`ContextManager.java` 常量区替换为：

```java
    /** 摘要输出上限（字符）：写入压缩器 system 提示词 */
    static final int SUMMARY_MAX_CHARS = 5000;
    /** 触发阈值（estimate >= maxContextTokens × THRESHOLD） */
    static final double THRESHOLD = 0.65;
    /** 保留区预算比例：保留区 token 预算 = maxContextTokens × THRESHOLD × 本值 = 0.13×max
     *  （触发量的 20% 留给保留区，其余 80% 为压缩目标） */
    static final double KEEP_BUDGET_RATIO = 0.2;
    /** 保底保留的最近原子组数：最新 1 组无条件保留（哪怕自身超预算）——当前任务上下文不可丢 */
    static final int KEEP_MIN_GROUPS = 1;
    /** 事前收益门槛：可压量 ≥ maxContextTokens × 本值 才值得压缩
     *  （摘要输出本身 ~2-3.5k token，压少了净收益为负） */
    static final double MIN_COMPRESSIBLE_RATIO = 0.05;
    /** 危险区豁免：estimate ≥ maxContextTokens × 本值（贴近窗口）时无视门槛强制压缩保命 */
    static final double FORCE_COMPRESS_RATIO = 0.85;
```

`compress(messages)` 中 `int take = takeCount(groups);` 改为：

```java
        int take = groups.size() - keepCount(groups);
```

`takeCount(...)` 整个方法替换为下面两个（并删除其 Javadoc）：

```java
    /** 保留的原子组数：从最新组往前累加 token，累计 ≤ 预算的最大组数（下限 KEEP_MIN_GROUPS=1，无上限）。
     *  组内是协议不可拆单位（assistant(tool_calls)+tool 配对），只能整组保留/整组压缩。 */
    private int keepCount(List<List<Message>> groups) {
        long budget = (long) (maxContextTokens * THRESHOLD * KEEP_BUDGET_RATIO);
        long acc = 0;
        int keep = 0;
        for (int i = groups.size() - 1; i >= 0; i--) {
            long t = TokenCounter.estimateMessages(groups.get(i));
            if (keep >= KEEP_MIN_GROUPS && acc + t > budget) break;
            acc += t;
            keep++;
        }
        return Math.max(keep, KEEP_MIN_GROUPS);
    }

    /** 本次压缩能压掉的 token 量（＝将被并入摘要的组合计；take = 0 时为 0）：事前收益门槛的输入 */
    public long compressibleTokens(List<Message> messages) {
        List<List<Message>> groups = chunkGroups(messages);
        int take = groups.size() - keepCount(groups);
        long acc = 0;
        for (int i = 0; i < take; i++) acc += TokenCounter.estimateMessages(groups.get(i));
        return acc;
    }

    /** 本次是否值得压缩（调用方须先确认 shouldCompress 为 true）：
     *  危险区（estimate ≥ 85%×max）无条件压；否则须可压量 ≥ 5%×max（防零收益空转、防压出净增）。
     *  只跳过"本次"——任务推进后旧组滑出保留区、可压量增长，随时可再次压缩。 */
    public boolean worthCompressing(List<Message> messages) {
        if (estimate(messages) >= maxContextTokens * FORCE_COMPRESS_RATIO) return true;
        return compressibleTokens(messages) >= maxContextTokens * MIN_COMPRESSIBLE_RATIO;
    }
```

同时更新类头 Javadoc 中「压缩比例 0.8 / 至少保留最近 6 个原子组」的表述为「保留区预算 0.13×max、保底最新 1 组」。

- [ ] **Step 4: 跑到绿**

Run: `mvn -q test -Dtest=ContextManagerTest -DfailIfNoTests=false`
Expected: 退出码 0（无输出）。若某用例前提不成立，按 stderr/报告调整**测试里的构造数字**（不要改算法常量）。

- [ ] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/context/ContextManager.java src/test/java/com/minion/core/context/ContextManagerTest.java
git commit -m "feat(context): 压缩保留区 token 预算化 + 收益判定（可压量/危险区）"
```

---

### Task 2: AgentLoop v2 接线（收益门槛 + 提示去重）

**Files:**
- Modify: `src/main/java/com/minion/core/agent/AgentLoop.java`（字段改名、循环顶判定、提示辅助方法）
- Test: `src/test/java/com/minion/core/agent/AgentLoopCompactTest.java`

**Interfaces:**
- Consumes: `ContextManager.shouldCompress` / `worthCompressing` / `compressibleTokens`（Task 1）
- Produces: `private void warnCompressIneffectiveOnce(String msg)`；字段 `private boolean ineffectiveWarnedThisTurn`（**只去重提示，不阻塞压缩**）

- [ ] **Step 1: 改造测试（先红）**

1) 把 v1 用例 `autoCompress_ineffective_blocksRepeatAndWarns` 整体替换为：

```java
    /** v2：可压量不足 → 事前跳过（不调压缩 LLM）+ 提示只出现一次（每回合一次，不阻塞后续压缩） */
    @Test
    public void autoCompress_lowYield_skipsWithoutLlmAndWarnsOnce() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】被压缩的历史";
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        RecordingUi ui = new RecordingUi();
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        ContextManager cm = new ContextManager(200000, llm, 0); // 阈值 130k、门槛 10k、预算 26k、危险区 170k
        AgentLoop loop = new AgentLoop(llm, registry,
                new SystemPromptBuilder(tmp.getRoot().getPath() + "/project.md"),
                confirm, ui, cm,
                new Workspace(tmp.getRoot().getPath()),
                Session.create(tmp.getRoot().getPath(), "test-model"));
        loop.retryPolicy = new RetryPolicy(10, 10, 60000);
        loop.roundLimit = 20;
        // pinned 105k token（420000 字符）+ 4 组各 3k（12000 字符）：可压量 ≈ 3k < 门槛 10k，estimate ≈ 132k ∈ [130k,170k)
        Message pin = Message.user(ascii(420000));
        pin.pinned = true;
        loop.messages().add(pin);
        for (int i = 0; i < 4; i++) loop.messages().add(Message.user(ascii(12000)));
        assertTrue("场景前提：超阈值", cm.shouldCompress(loop.messages()));
        assertFalse("场景前提：可压量不足门槛", cm.worthCompressing(loop.messages()));
        // 3 轮小工具（可压量增长但始终 < 门槛 10k）+ 1 轮收尾：每次请求前都走「暂缓」分支
        for (int r = 0; r < 3; r++) {
            ToolCall tc = new ToolCall();
            tc.id = "s" + r;
            tc.name = "example";
            tc.arguments = "{\"text\":\"hi\"}";
            llm.addTurnWithTools(Collections.singletonList(tc), null);
        }
        llm.addTurn("完成"); // 暂缓不阻塞本轮请求
        loop.runUserTurn(ascii(60000)); // 本轮 user 15k token
        assertTrue("可压内容不足 → 不调压缩 LLM: " + llm.completeChatRequests,
                llm.completeChatRequests.isEmpty());
        assertEquals("「暂缓」提示每回合只一次: " + ui.warnings,
                1, ui.warnings.stream().filter(w -> w.contains("自动压缩暂缓")).count());
    }

    /** ascii 辅助（Task 内新增，供多轮/跳过用例共用） */
    private static String ascii(int n) {
        StringBuilder sb = new StringBuilder(n);
        for (int i = 0; i < n; i++) sb.append('x');
        return sb.toString();
    }
```

2) 新增「长任务回合可多次压缩」用例（核心回归，**不得**被回合级标志卡死）：

```java
    /** 长任务回合：任务推进使可压量增长 → 压缩可发生多次（回归"回合内只能用一次"） */
    @Test
    public void autoCompress_longTurn_compressesMultipleTimes() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        RecordingUi ui = new RecordingUi();
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        ContextManager cm = new ContextManager(60000, llm, 0); // 阈值 39k、门槛 3k、预算 7.8k、危险区 51k
        AgentLoop loop = new AgentLoop(llm, registry,
                new SystemPromptBuilder(tmp.getRoot().getPath() + "/project.md"),
                confirm, ui, cm,
                new Workspace(tmp.getRoot().getPath()),
                Session.create(tmp.getRoot().getPath(), "test-model"));
        loop.retryPolicy = new RetryPolicy(10, 10, 60000);
        loop.roundLimit = 20;
        // 每轮 1 个 example 工具，text 30000 字符 → 组 ≈ 15k token（arguments 7.5k + tool 结果 7.5k）
        for (int r = 0; r < 5; r++) {
            ToolCall tc = new ToolCall();
            tc.id = "m" + r;
            tc.name = "example";
            tc.arguments = "{\"text\":\"" + ascii(30000) + "\"}";
            llm.addTurnWithTools(Collections.singletonList(tc), null);
        }
        llm.addTurn("完成");
        loop.runUserTurn("跑长任务");
        int n = llm.completeChatRequests.size();
        assertTrue("长任务回合内应能多次压缩（至少 2 次），实际 " + n, n >= 2);
        assertTrue("压缩有效提示出现: " + ui.warnings,
                ui.warnings.stream().anyMatch(w -> w.contains("降低至") || w.contains("仍占")));
        assertEquals("完成", loop.messages().get(loop.messages().size() - 1).content);
    }
```

需确认该文件已 import：`java.util.Collections`、`com.minion.core.llm.ToolCall`、`com.minion.core.llm.Message`（若缺则补）。

3) 新增「危险区强制压缩」用例：

```java
    /** 危险区（≥85%×max）：可压量不足门槛也强制压缩一次（保命） */
    @Test
    public void autoCompress_dangerZone_compressesDespiteLowYield() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        RecordingUi ui = new RecordingUi();
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        ContextManager cm = new ContextManager(1200, llm, 0); // 阈值 780、门槛 60、预算 156、危险区 1020
        AgentLoop loop = new AgentLoop(llm, registry,
                new SystemPromptBuilder(tmp.getRoot().getPath() + "/project.md"),
                confirm, ui, cm,
                new Workspace(tmp.getRoot().getPath()),
                Session.create(tmp.getRoot().getPath(), "test-model"));
        loop.retryPolicy = new RetryPolicy(10, 10, 60000);
        loop.roundLimit = 20;
        Message pin = Message.user(ascii(4000)); // 1000 token → estimate ≥ 危险区
        pin.pinned = true;
        loop.messages().add(pin);
        for (int i = 0; i < 30; i++) loop.messages().add(Message.user(i < 9 ? "x" : "问" + (i % 10)));
        assertTrue("场景前提：超阈值", cm.shouldCompress(loop.messages()));
        assertTrue("场景前提：进危险区", cm.estimate(loop.messages()) >= 1020);
        assertTrue("场景前提：强制压", cm.worthCompressing(loop.messages()));
        llm.addTurn("压缩后回复");
        loop.runUserTurn("触发压缩");
        assertEquals("危险区强制压缩一次", 1, llm.completeChatRequests.size());
    }
```

- [ ] **Step 2: 跑到红**

Run: `mvn -q test -Dtest=AgentLoopCompactTest -DfailIfNoTests=false`
Expected: 编译通过但用例红（`autoCompress_lowYield_…` 会因为 v1 逻辑仍会尝试压缩而失败；`autoCompress_longTurn_…` 因 v1 回合级标志只压一次而失败）。

- [ ] **Step 3: 实现 AgentLoop 改造**

1) 字段（`sessionTmpDir` 之后那段）替换为：

```java
    /** 压不动提示去重（回合内一次）：仅控制提示频率，**不阻塞压缩**——
     *  v2 语义下压缩是否发生由 ContextManager.worthCompressing 判定，任务推进后可压量增长即自动重试 */
    private boolean ineffectiveWarnedThisTurn = false;
```

2) `runUserTurn` 开头重置行改为：

```java
        ineffectiveWarnedThisTurn = false; // 新回合重置提示去重（不阻塞压缩）
```

3) 循环顶压缩段替换为：

```java
                if (contextManager != null && contextManager.shouldCompress(session.messages)) {
                    if (contextManager.worthCompressing(session.messages)) {
                        ui.onCompressingChanged(true);
                        CompressOutcome outcome;
                        try {
                            outcome = compressWithRetry(false); // 自动压缩：失败文案带"自动"与"本轮已停止"
                        } finally {
                            ui.onCompressingChanged(false);
                        }
                        if (outcome == CompressOutcome.OK) {
                            int pct = (int) (contextManager.estimate(session.messages) * 100
                                    / contextManager.maxTokens());
                            if (contextManager.shouldCompress(session.messages)) {
                                // 压后仍超阈值：保留区保底组自身超预算（如最新一轮大输出）或常驻内容过大
                                warnCompressIneffectiveOnce("自动压缩后上下文仍占" + pct
                                        + "%（大输出占满保留区）；有新内容可压时自动重试");
                            } else {
                                ui.onWarning("自动压缩已完成，上下文降低至" + pct + "%");
                            }
                            pushContextStats(); // 压缩完成：进度圈回落
                        } else if (outcome == CompressOutcome.FAILED) {
                            break; // 压缩失败/用户中断：中止本轮，不发送请求（失败已 onError 提示）
                        }
                        // NOTHING：极端竞态下无可压缩，继续本轮请求（超窗由服务端报错兜底）
                    } else {
                        // 事前收益门槛不足（可压量 < 5%×max）：本次跳过，不调 LLM；只提示一次，不阻塞后续
                        int pct = (int) (contextManager.estimate(session.messages) * 100
                                / contextManager.maxTokens());
                        warnCompressIneffectiveOnce("自动压缩暂缓：可压内容不足（最近内容与常驻提示词已占"
                                + pct + "%）；有新内容可压时自动重试");
                    }
                }
```

4) 在 `compactNow()` 之前新增辅助方法：

```java
    /** 压不动/暂缓提示去重：同一回合只提示一次（不阻塞压缩，可压量增长后仍会自动重试） */
    private void warnCompressIneffectiveOnce(String msg) {
        if (ineffectiveWarnedThisTurn) return;
        ineffectiveWarnedThisTurn = true;
        ui.onWarning(msg);
    }
```

> `compactNow()`（手动 /compact）**不加任何门槛**，保持现状。

- [ ] **Step 4: 跑到绿**

Run: `mvn -q test -Dtest=AgentLoopCompactTest -DfailIfNoTests=false`
Expected: 退出码 0。预期既有用例无需改动（v1 用例已在 Step 1 被替换）；若确有失败，只按其新语义修正**测试断言/场景数字**，不得改业务逻辑。

- [ ] **Step 5: 跑受影响的两个类 + 提交**

Run: `mvn -q test -Dtest='AgentLoopTest,AgentLoopCompactTest' -DfailIfNoTests=false`
Expected: 退出码 0。

```bash
git add src/main/java/com/minion/core/agent/AgentLoop.java src/test/java/com/minion/core/agent/AgentLoopCompactTest.java
git commit -m "feat(agent): 自动压缩改用收益门槛 + 提示去重（v2，长任务回合可多次压缩）"
```

---

### Task 3: SubAgentLoop v2 接线

**Files:**
- Modify: `src/main/java/com/minion/core/agent/SubAgentLoop.java`（字段改名、循环顶判定、压缩成功分支）
- Test: `src/test/java/com/minion/core/agent/SubAgentLoopTest.java`

**Interfaces:**
- Consumes: `ContextManager.shouldCompress` / `worthCompressing`（Task 1）
- Produces: 字段 `private boolean ineffectiveWarned`（子代理全程一次提示，不阻塞压缩）

- [ ] **Step 1: 改造测试（先红）**

1) 把 v1 用例 `compressIneffective_blocksRepeat` 整体替换为：

```java
    /** v2：可压量不足 → 暂缓（不调压缩 LLM）+ 子代理全程只提示一次；危险区（≥85%×max）仍强制压缩 */
    @Test
    public void compressLowYield_skipsWithoutLlmAndWarnsOnce() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        RecordingUi ui = new RecordingUi();
        ContextManager cm = new ContextManager(1200, llm, 0); // 阈值 780、门槛 60、预算 156、危险区 1020
        SubAgentLoop sub = new SubAgentLoop("主系统提示", "任务", tmp.getRoot().getPath(),
                llm, registry, confirm, ui, null, 1);
        sub.contextManager = cm;
        Message pin = Message.user(ascii(3000)); // 750 token
        pin.pinned = true;
        sub.messages().add(pin);
        for (int i = 0; i < 30; i++) sub.messages().add(Message.user(i < 9 ? "x" : "问" + (i % 10)));
        assertTrue("场景前提：超阈值", cm.shouldCompress(sub.messages()));
        assertFalse("场景前提：可压量不足门槛", cm.worthCompressing(sub.messages()));
        ToolCall tc = new ToolCall();
        tc.id = "t1";
        tc.name = "example";
        tc.arguments = "{\"text\":\"hi\"}";
        llm.addTurnWithTools(Collections.singletonList(tc), null);
        llm.addTurn("子任务完成");
        assertEquals("子任务完成", sub.run());
        assertTrue("可压量不足 → 不调压缩 LLM: " + llm.completeChatRequests,
                llm.completeChatRequests.isEmpty());
        assertEquals("「暂缓」提示全程只一次: " + ui.subNotices,
                1, ui.subNotices.stream().filter(n -> n.contains("自动压缩暂缓")).count());
    }

    /** ascii 辅助 */
    private static String ascii(int n) {
        StringBuilder sb = new StringBuilder(n);
        for (int i = 0; i < n; i++) sb.append('x');
        return sb.toString();
    }

    /** v2：危险区（≥85%×max）→ 可压量不足门槛也强制压缩一次（保命） */
    @Test
    public void compressForceZone_compressesDespiteLowYield() throws Exception {
        Config config = Config.load(tmp.getRoot().toPath());
        FakeLlmClient llm = new FakeLlmClient();
        llm.compressResult = "【摘要】要点";
        ToolRegistry registry = new ToolRegistry();
        registry.register(new com.minion.core.tools.example.ExampleTool());
        ConfirmGate confirm = new ConfirmGate(config, new FakeConfirmUi(ConfirmUi.Decision.APPROVE));
        RecordingUi ui = new RecordingUi();
        ContextManager cm = new ContextManager(1200, llm, 0); // 危险区 1020
        SubAgentLoop sub = new SubAgentLoop("主系统提示", "任务", tmp.getRoot().getPath(),
                llm, registry, confirm, ui, null, 1);
        sub.contextManager = cm;
        Message pin = Message.user(ascii(4000)); // 1000 token
        pin.pinned = true;
        sub.messages().add(pin);
        for (int i = 0; i < 30; i++) sub.messages().add(Message.user(i < 9 ? "x" : "问" + (i % 10)));
        assertTrue("场景前提：可压量不足但进危险区",
                cm.worthCompressing(sub.messages()) && cm.compressibleTokens(sub.messages()) < 60);
        llm.addTurn("子任务完成");
        assertEquals("子任务完成", sub.run());
        assertEquals("危险区强制压缩一次", 1, llm.completeChatRequests.size());
    }
```

2) 既有用例 `subAgent_compressesOverThreshold_withSubAgentPrompt` 无需修改（max=200、12 组：可压量 48 ≥ 门槛 10，压后 < 阈值 → 「已压缩上下文（降低至 X%）」文案不变）；若实际红，按新语义微调**场景数字**。

- [ ] **Step 2: 跑到红**

Run: `mvn -q test -Dtest=SubAgentLoopTest -DfailIfNoTests=false`
Expected: 新用例红（v1 仍会尝试压缩/缺 `worthCompressing` 判定接线）。

- [ ] **Step 3: 实现 SubAgentLoop 改造**

1) 字段替换：

```java
    /** 压不动/暂缓提示去重（子代理全程一次）：仅控制提示频率，**不阻塞压缩**——
     *  压缩是否发生由 ContextManager.worthCompressing 判定，任务推进后可压量增长即自动重试 */
    private boolean ineffectiveWarned = false;
```

2) `run()` 循环顶压缩检查替换为：

```java
                // 上下文压缩检查点（与主代理同策略）：超阈值且值得压 → 压缩（失败中止并返回失败文本）；
                // 可压量不足门槛 → 暂缓（不调 LLM），提示一次后静默跳过（有新内容可压时自动重试）
                if (contextManager != null && contextManager.shouldCompress(messages)) {
                    if (contextManager.worthCompressing(messages)) {
                        String fail = compressSubContext();
                        if (fail != null) {
                            if (Thread.currentThread().isInterrupted()) {
                                // 压缩等待期间被停止：与其他中断路径同文案（spec 4.2「中断 → 子代理已中断」）
                                ui.onSubAgentNotice(no, "已中断");
                                return "子 agent 已中断";
                            }
                            ui.onSubAgentNotice(no, "上下文压缩失败：" + fail);
                            return "子代理失败: 上下文压缩失败（" + fail + "）";
                        }
                    } else if (!ineffectiveWarned) {
                        ineffectiveWarned = true;
                        int pct = (int) (contextManager.estimate(messages) * 100
                                / contextManager.maxTokens());
                        ui.onSubAgentNotice(no, "自动压缩暂缓：可压内容不足（最近内容与常驻提示词已占 "
                                + pct + "%）；有新内容可压时自动重试");
                    }
                }
```

3) `compressSubContext()` 成功分支替换为：

```java
        if (r.outcome == ContextCompressor.Outcome.OK) {
            messages.clear();
            messages.addAll(r.messages); // 摘要置前 + pinned 任务提示词常驻（压缩结果由 ContextManager 保证）
            int pct = (int) (contextManager.estimate(messages) * 100 / contextManager.maxTokens());
            if (contextManager.shouldCompress(messages)) {
                // 压后仍超阈值：保留区保底组自身超预算（如最新一轮大输出）或常驻内容过大——
                // 只提示一次，不阻塞后续（可压量增长后自动重试）
                if (!ineffectiveWarned) {
                    ineffectiveWarned = true;
                    ui.onSubAgentNotice(no, "压缩后上下文仍占 " + pct
                            + "%（大输出占满保留区）；有新内容可压时自动重试");
                }
            } else {
                // spec 4.2：成功提示带压缩后百分比（算法与主代理自动压缩一致）
                ui.onSubAgentNotice(no, "已压缩上下文（降低至 " + pct + "%）");
            }
            return null;
        }
```

- [ ] **Step 4: 跑到绿**

Run: `mvn -q test -Dtest=SubAgentLoopTest -DfailIfNoTests=false`
Expected: 退出码 0。

- [ ] **Step 5: 提交**

```bash
git add src/main/java/com/minion/core/agent/SubAgentLoop.java src/test/java/com/minion/core/agent/SubAgentLoopTest.java
git commit -m "feat(agent): 子代理压缩改用收益门槛（v2，暂缓提示全程一次）"
```

---

### Task 4: 文档同步 + 全量验证

**Files:**
- Modify: `README.md`、`docs/ARCHITECTURE.md`、`docs/superpowers/specs/2026-09-14-压缩保留区预算化-design.md`（追加实施记录）
- Test: 全量 `mvn test`

**Interfaces:**
- Consumes: Task 1-3 全部落地
- Produces: 文档与代码一致；全量测试绿

- [ ] **Step 1: 同步 README**

把 `README.md:97` 的 `- **上下文压缩**：…` 整行替换为（关键词必须保留）：

```markdown
- **上下文压缩**：主代理与子代理同一压缩策略自动压缩（触发阈值 0.65×max 一致）；系统提示词/任务提示词不参与压缩（常驻），压缩指令为子代理定制版；压缩失败按瞬时错误长重试，重试耗尽才中止；**保留区按 token 预算**（0.13×max，200k 窗口约 26k token）从最新往前保留、**保底最新 1 组**（不再固定 6 组）；**收益门槛**：可压量 < 5%×max 时暂缓压缩（不调模型），提示「自动压缩暂缓：可压内容不足（最近内容与常驻提示词已占 X%）；有新内容可压时自动重试」——任务推进后可压量增长即自动重试，长任务回合内可多次压缩；占用 ≥ 85%×max 进危险区时无视门槛强制压缩保命；压缩后仍超阈值提示「自动压缩后上下文仍占 X%（大输出占满保留区）；有新内容可压时自动重试」（主代理每回合、子代理全程至多一条提示）；手动 /compact 不受门槛限制
```

- [ ] **Step 2: 同步 ARCHITECTURE**

4 处精确替换：

1) `docs/ARCHITECTURE.md:62`（AgentLoop 行）中的
   `**压缩无效防抖** \`compressIneffectiveThisTurn\`（压缩后仍超阈值 → 本回合不再自动压缩 + 如实提示，runUserTurn 开头重置；手动 /compact 不受限）`
   替换为
   `**压缩收益门槛**（超阈值且 \`ContextManager.worthCompressing\`：可压量 ≥5%×max 或已进危险区 ≥85%×max 才压；「暂缓/仍占」提示由 \`ineffectiveWarnedThisTurn\` 每回合去重，**不阻塞压缩**——可压量增长即自动重试；手动 /compact 不受限）`

2) `docs/ARCHITECTURE.md:63`（SubAgentLoop 行）中的
   `+ 压缩无效防抖 \`compressIneffective\`（全程不再重复压缩，提示一次）`
   替换为
   `+ 压缩收益门槛（与主代理同一判定；「暂缓/仍占」提示由 \`ineffectiveWarned\` 全程一次，不阻塞压缩）`

3) `docs/ARCHITECTURE.md:111`（ContextManager 行）中的
   `从最早组按 token 累加到 0.65×0.8 后整体压缩，且至少保留最近 6 组`
   替换为
   `保留区按 token 预算 0.13×max（比例 0.2×触发阈值）从最新往前保留、保底最新 1 组，其余早期组整体并入摘要；\`compressibleTokens\`（可压量）/ \`worthCompressing\`（5%×max 收益门槛、85%×max 危险区豁免）`

4) `docs/ARCHITECTURE.md` 第 7 节常量表「DB 工具结果超限落盘 tmpDir」行之后新增一行：

```markdown
| 压缩保留区预算（=max×0.65×0.2）/ 保底组数 / 收益门槛 / 危险区 | 0.13×max（配比 0.2）/ 1 组 / 5%×max / 85%×max | ContextManager |
```

- [ ] **Step 3: 追加设计文档实施记录**

在 `docs/superpowers/specs/2026-09-14-压缩保留区预算化-design.md` 末尾追加「## 7. 实施记录」：逐项列出落地文件、测试用例名、`mvn test` 全量结果（数字以实际为准），并注明与 v1 的差异（v1 回合级防抖已替换为 v2 提示去重）。

- [ ] **Step 4: 全量验证**

Run: `mvn test`
Expected: `BUILD SUCCESS`，`Tests run: … Failures: 0, Errors: 0`（用例总数以实际为准，应 ≥ 1147）。

- [ ] **Step 5: 打包验证（可选但推荐）**

Run: `mvn -q package -DskipTests`
Expected: 退出码 0，`target/minion-0.1.0.jar` 生成。

- [ ] **Step 6: 提交**

```bash
git add README.md docs/ARCHITECTURE.md "docs/superpowers/specs/2026-09-14-压缩保留区预算化-design.md"
git commit -m "docs: 压缩保留区预算化与 v2 收益门槛文档同步"
```

---

## 自审清单（写完计划后已核对）

- **规格覆盖**：2.1 常量→Task 1；2.2 算法+`compressibleTokens`→Task 1；2.3 行为变化→Task 1/2/3 用例；2.4 v2 门槛与提示→Task 2/3；2.5 v1 改造清单→Task 2/3；3.1-3.3 测试→Task 1/2/3；3.4 全局→Task 4；4 风险（危险区保命）→Task 2 用例；5 验收→Task 2/4 断言。
- **类型一致性**：`compressibleTokens` 返回 `long`；`worthCompressing` 参数/返回 `(List<Message>) -> boolean`；`keepCount` 为私有 `(List<List<Message>>) -> int`；字段名 `ineffectiveWarnedThisTurn`（AgentLoop）/`ineffectiveWarned`（SubAgentLoop）在测试与实现中一致。
- **已知取舍**：测试里的 token 数字均配「场景前提」断言（如 `assertTrue("场景前提：…", cm.shouldCompress(...))`），数字漂移时报前提失败而非误报逻辑错误。
